# Worklog — sdílený deník změn (Claude + Codex + člověk)

Každý agent sem PŘIDÁ záznam hned po každé dokončené práci (změna kódu, deploy, zásah do prod DB, rozhodnutí). Nové záznamy NAHORU. Formát: datum, kdo, co, stav (lokálně / NASAZENO / prod-data).

---

## 2026-07-22

- **[Codex] Produkční Git checkpoint a oddělení lokálního WIP — lokálně, NENASAZENO** — Aktivní Contabo release `webos-1784739468656491800` byl read-only stažen, hashově porovnán a uložen jako přesný runtime checkpoint `cf05812` s tagem `production/webos-1784739468656491800`; checkpoint prošel 517/517 testy a frontend syntaxí. Zbývající lokální práce byla beze ztráty uložena do větve `codex/local-wip-20260722` v commitu `9a49f93` (520/520 testů). `main` byl fast-forwardnut na produkční checkpoint. Zastaralý Fly.io návod a odkazy na zrušené `flyctl` byly nahrazeny aktuálním Contabo postupem; kompatibilní legacy hlavičky v kódu zůstaly beze změny chování. Bez deploye, restartu služby nebo změny produkčních dat.

- **[Codex] Celková kontrola stavu projektu — lokálně, rozhodnutí bez zásahu** — Veřejný health vrátil HTTP 200 se stavem `app=ok`, `db=ok`, maintenance off a alerting configured; lokální predeploy prošel 520/520 testy, frontend balance + `node --check` a `pip check` bez chyb. Projekt je provozně zdravý, ale repozitář není připravený na slepý deploy: pracovní strom má 38 změn (22 runtime souborů v `app/`/`web/`), přestože `main` je stále shodný s `origin/main` na commitu `53ae0b0`, zatímco worklog eviduje novější produkční release `webos-1784739468656491800`. Před dalším deployem je potřeba oddělit WIP, srovnat skutečný produkční stav do Git commitů a znovu spustit bránu. Ignorované lokální soubory s tajnými údaji nejsou trackované. Bez deploye, změny produkce nebo produkčních dat.

- **[Codex] Potvrzena možnost hostovat další izolované služby na Contabo VPS — lokálně, rozhodnutí bez zásahu** — VPS má podle poslední živé kontroly 6 vCPU, 11 GiB RAM a přibližně 85 GiB volného disku; současný `webos.service` proto nevylučuje několik menších služeb. Veřejné projekty musí mít vlastní doménu/reverzní proxy, systemd službu, limity zdrojů a nesmí sdílet databázi ani port aplikace `zurys.live`. Bez změny VPS, deploye nebo produkčních dat.

- **[Codex] Historické přiřazení requestové špičky účtům není dostupné — produkce read-only, rozhodnutí bez zásahu** — Anti-DDoS měřič drží pouze IP a jen posledních 5 minut v paměti procesu (`ddos._hits`); po restartu se data smažou a tabulka neobsahuje user ID ani request log. Uvicorn journal zapisuje pro požadavek Cloudflare proxy IP, ne `CF-Connecting-IP`, proto z něj nelze zpětně sestavit seznam účtů. Z historické špičky lze doložit pouze celkových 7 138 requestů/5 min, 17× HTTP 503 a tehdy pozorované silné zdroje uvedené v dřívějších záznamech; úplný či důkazný seznam účtů neexistuje. Bez deploye, změny produkčních dat či postihu účtu.

- **[Codex] Nepotvrzené přiřazení requestové špičky zamítnuto — lokálně, rozhodnutí bez zásahu** — Není podklad pro tvrzení, že špičku 7 138 requestů/5 min způsobili `op_domy`, `MiSaK750` nebo účty spojované s Kakaduem. Předchozí kontroly `op_domy` a `MiSaK750` nenalezly dostatečný automatizační vzor; pro incident je doložen celkový globální polling přibližně 89 aktivních účtů za 5 minut. Jména proto nebudou uvedena jako viníci v žádném oznámení. Bez deploye, změny produkčních dat či postihu účtu.

- **[Codex] Oprava komunikace k requestové špičce — lokálně, rozhodnutí** — Předchozí odpověď nesprávně zpochybnila uživatelem označený den podle členění worklogu. Potvrzený incident je requestová špička 7 138 požadavků za 5 minut (tedy přes 6 tisíc), která při limitu 256 souběžných spojení vyvolala 17× HTTP 503 a pomalé/selhávající načítání. Nešlo o pád celé služby: Kick webhooky zůstaly 145/145 HTTP 200. Následně byl omezen polling a concurrency limit zvýšen na 512; bez nového deploye nebo změny produkčních dat.

- **[Codex] Kontrola údajnému výpadku 21. 7. — produkce read-only, rozhodnutí bez zásahu** — Worklog pro 21. 7. zaznamenává jen plánované atomické restarty při deployích; po každém byl veřejný health `app/db=ok`, maintenance off a service error log prázdný. Nenachází se záznam o produkční špičce, pádu ani HTTP 5xx z tohoto dne. Potvrzená requestová špička je až 22. 7.: 7 138 požadavků za 5 minut, 17× HTTP 503 při limitu 256 souběžných spojení; následné omezení pollingu a limit 512 ji odstranily. Bez deploye, změny produkčních dat nebo konfigurace.

- **[Codex] Samostatný projekt `accounts-manager` vytvořen — lokálně, NENASAZENO** — `C:\Users\Administrator\Desktop\retoš` byl na výslovný pokyn přesunut do `C:\Users\Administrator\Desktop\accounts-manager`; zde vznikl nový nezávislý Git repozitář na větvi `main` a `.gitignore` pro `.env`, data, logy a `node_modules`. Kontrola syntaxe pasivního serveru i heartbeat userscriptu prošla. Projekt je mimo `webos`, takže nemůže být zahrnut do `webos\deploy.py`; žádná změna VPS ani `zurys.live`.

- **[Codex] Rozhodnutí o izolaci Control Centeru — lokálně, bez deploye** — Control Center zůstane samostatným projektem mimo `C:\Users\Administrator\webos`, s vlastním repozitářem a vlastním nasazením; nikdy se nebude přidávat do `webos` ani nasazovat přes `webos\deploy.py`. Tím se jeho kód nemůže dostat do payloadu `zurys.live`. Žádná změna produkce ani VPS.

- **[Codex] Control Center převeden na pasivní heartbeat — lokálně mimo `webos`, NENASAZENO** — V `C:\Users\Administrator\Desktop\retoš\control-center` byly odstraněny všechny čtyři Tampermonkey varianty s automatickým ovládáním; nově existuje pouze `userscript/zurys-heartbeat.user.js`, který každých 15 s odesílá minimální heartbeat a nečte herní stav, nekliká, nenaviguje ani nepřijímá vzdálené akce. Backend už příkazy nevytváří ani endpoint `/api/command` nemá, dashboard je pouze pro zobrazení heartbeatů a backend naslouchá výhradně na `127.0.0.1`. Ověřeno syntaxí a HTTP testem: neověřený heartbeat 401, ověřený 200, dashboard ho vidí a command endpoint vrací 404. Žádná změna ani deploy na `zurys.live`.

- **[Codex] Kontrola kapacity Contabo VPS — prod-infra read-only, rozhodnutí** — Živá kontrola potvrzuje 6 vCPU, 11 GiB RAM (10 GiB dostupných), disk 96 GiB, z toho 12 GiB použitých / 85 GiB volných (13 %). `webos.service` je active. Server má velkou rezervu pro několik malých, izolovaných služeb; před přidáním veřejné aplikace je potřeba oddělit její doménu/reverzní proxy a limity zdrojů od `zurys.live`. Bez změny konfigurace, deploye nebo produkčních dat.

- **[Codex] Výjimka z Turnstile checkpointu Zahrádky/Statku pro `SpartanskySlavista` a `SousedsVrtackou` — prod-data** — Na výslovný pokyn byly oba existující účty atomicky přidány do `automation_verifications` s výjimkou do `9999-12-31T23:59:59+00:00`; přeskočí se pouze Turnstile checkpoint, limit 30 akcí/min a ostatní ochrany zůstávají. Před zásahem vznikla konzistentní záloha `/data/backups/app-before-farm-turnstile-exempt-20260722T190105.db`; `quick_check=ok`, health `app/db=ok`, maintenance off. Bez deploye, banu, timeoutu nebo změny bodů.
- **[Codex] Prověření možné shody účtů `Matyseczek` a `M4tySeek` — produkce read-only, rozhodnutí bez zásahu** — Jde o dva samostatné účty (ID 452 a 976) s odlišným Kick ID a bez propojeného e-mailu. Produkční relace nemají žádnou společnou uloženou IP, zařízení nemají žádný shodný fingerprint a Steam Trade URL nejsou stejné; jediný překryv je jeden běžný přesný user-agent, který sám o sobě není identifikační důkaz. Data proto nepotvrzují, že by šlo o stejnou osobu, a nejsou podkladem pro ban, omezení ani sloučení účtů. Bez změny produkčních dat, deploye nebo zásahu do účtů.
- **[Codex] Volitelná délka nabídky komunitního Trhu — NASAZENO (cache 2026071293, release `webos-1784739468656491800`)** — Prodejce volí 6 h, 12 h, 24 h, 3 dny nebo 7 dní; server hodnotu validuje (1 h–7 dní), ukládá do `market_submissions` a administrace ji při schválení použije jako skutečnou délku nabídky. Migrace přidala `duration_minutes` s bezpečným výchozím 1440 pro dosavadní čekající nabídky. Izolovaný kandidát byl sestaven přímo z aktivního release `webos-1784722190416491400`; kompletní predeploy 501/501 a frontend syntax/balance prošly. Atomický release, restart, veřejný health (`app/db=ok`, maintenance off), kontrola migrace, SHA-256 všech pěti runtime souborů, public cache/UI i Cloudflare Purge Everything uspěly; service error log po restartu je prázdný. Bez ruční změny produkčních dat.

- **[Codex] Volitelná délka nabídky komunitního Trhu — lokálně, NENASAZENO** — Prodejce při odeslání nabídky nově volí 6 h, 12 h, 24 h, 3 dny nebo 7 dní (výchozí 24 h). Hodnota se validuje na serveru v rozmezí 1 h–7 dní, ukládá do `market_submissions`, je viditelná administraci a při schválení určí skutečný konec veřejné nabídky; staré čekající návrhy bezpečně používají výchozích 24 h. Cílený tok odeslání → schválení → 3denní nabídka včetně neplatné délky prošel 21/21 testů, syntax `web/app.js` i kontrola whitespace diffu prošly. Bez deploye a změny produkčních dat.

- **[Codex] Kontrola přetrvávajícího Turnstile modalu — produkce read-only, rozhodnutí bez změny** — Všechny 28 dříve nastavených účtů byly znovu vyhodnoceny přímo stejnou produkční funkcí `automation_checkpoint`: 28/28 má DB výjimku, 0/28 má `required=true` a nejvyšší aktuální risk skóre 55 je správně přeskočeno. Screenshot odpovídá logovanému `POST /api/farm/collect` s HTTP 428 v 18:45:22 CEST; časově shodná aktivní session je `Ksiltovka`, který v operátorem dodaném seznamu není. Výjimka proto funguje správně pro zadané účty a na `Ksiltovka` nebyla bez dalšího pokynu rozšířena. Bez deploye a změny produkčních dat.
- **[Codex] Kontrola nedostupnosti webu — produkce read-only** — Veřejný web `https://zurys.live/` v době kontroly vrátil HTTP 200 za 0,27 s a `https://zurys.live/api/monitor/healthz` HTTP 200 za 0,13 s přes Cloudflare; DNS vrací očekávané Cloudflare adresy. Nejde tedy o celkový výpadek aplikace. Přímý SSH přístup na Contabo z této relace není autorizovaný (`Permission denied (publickey)`), proto nelze ověřit případný lokální IP ban ani serverové logy. Bez změny kódu, deploye a produkčních dat.
- **[Codex] Výjimka z Turnstile checkpointu Zahrádky/Statku pro 28 účtů — prod-data** — Na výslovný pokyn byly do `automation_verifications` atomicky zapsány trvalé výjimky do `9999-12-31T23:59:59+00:00` pro 28 nalezených účtů včetně aliasů `kakaduczech` (zadaný `KakaduCzech`) a `SituaceKramne` (zadaný `situacekramne`). Výjimka přeskočí pouze Turnstile kontrolu před výnosem ze Zahrádky/Statku; společný limit 30 farmářských akcí/min a ostatní anticheat ochrany zůstávají. `KakaduChorvatsky` nebyl v produkci nalezen ani pod podobným názvem, proto změněn nebyl. Před zásahem vznikla konzistentní záloha `/data/backups/app-before-farm-turnstile-exempt-20260722T183710.db`; auditní záznam #34062 `automation_checkpoint.exempt` uvádí 28 účtů. Následná kontrola potvrdila 28/28 řádků, `quick_check=ok`, veřejný health `app/db=ok`, maintenance off. Bez deploye, banu, timeoutu nebo změny bodů.
- **[Codex] Aukční vizuál `FACEIT PREMIUM GIVEAWAY` — lokálně** — Vygenerován čtvercový e-sport thumbnail pro aukční nabídku: tmavý grafitový podklad, oranžové akcenty a dobře čitelný hlavní text bez doplnění nevyžádané délky předplatného. Bez změny kódu, deploye a produkčních dat.
- **[Codex] Třívrstvá ochrana Zahrádky a Statku proti skriptům — NASAZENO (původně cache 2026071291/release `webos-1784721936147035600`, aktuálně zachováno v cache 2026071292/release `webos-1784722190416491400`)** — V Cloudflare byl přes přihlášený Chrome vytvořen managed Turnstile widget `ZURYS farm checkpoint` pouze pro `zurys.live`; site/secret key byly bez zveřejnění zapsány do `/opt/webos/env` s právy 600 a proces načetl všechny 3 `WEBOS_TURNSTILE_*` proměnné. Původní env je v `/opt/webos/env.pre-turnstile-20260722T140403`, konzistentní DB záloha před migrací v `/data/backups/app-before-antiscript-20260722T140510.db` (`integrity_check=ok`). Izolovaný kandidát z aktivní produkce prošel 499/499 testy; atomický deploy, cache bump, health a Cloudflare purge uspěly. Migrace vytvořila `automation_verifications`, DB `quick_check=ok`, Siteverify z VPS je dosažitelný a správně odmítl neplatný smoke token. Veřejná cache obsahuje checkpoint handler, explicitní Turnstile loader/modal i CSP `script-src` + `frame-src`; endpoint bez relace vrací 401. Následující souběžný MOD deploy byl správně postaven z tohoto release: kritické backendové soubory ochrany zůstaly SHA-256 shodné a frontendové kontrakty jsou v cache 1292 zachované. Po obou restartech se třikrát objevil přechodný existující `sqlite3 database is locked` na nesouvisejícím `GET /api/auctions`; od posledního výskytu následovalo 2 266 requestů bez další HTTP 500/503, služba je active bez restartu a Kick webhook 50× HTTP 200 / 0× 503. Produkční UI se v Chrome reloadlo, ale čtení DOM opakovaně timeoutovalo, proto interaktivní zobrazení challenge není označeno za vizuálně ověřené. Bez banu, timeoutu nebo ruční změny uživatelských/herních dat.
- **[Codex] Role MOD omezena pouze na timeouty — NASAZENO (cache 2026071292, release `webos-1784722190416491400`)** — Kvůli souběžnému deployi jiné práce byl kandidát znovu sestaven z nového aktivního release `webos-1784721936147035600`; tím zůstaly zachovány všechny právě nasazené změny a payload proti němu měnil pouze `app/config.py`, `app/routers/admin.py` a `web/app.js`. MOD má jen seznam uživatelů a může dát/zrušit timeout běžnému uživateli; body, bany, role, flags, admin metadata, blok sázek, odměny, objednávky, hry a predikce server odmítá a UI je nenabízí. Izolovaný predeploy 73/73 + frontend syntax/balance prošel, atomický restart i Cloudflare purge jsou OK, všechny runtime soubory jsou SHA-256 shodné s kandidátem, veřejný JS obsahuje nové oprávnění, health `app/db=ok`, maintenance off a service error log je prázdný. Bez změny produkčních dat.
- **[Codex] Role MOD omezena pouze na timeouty — lokálně, NENASAZENO** — MOD má v administraci už jen seznam uživatelů pro výběr cíle a může dát nebo zrušit timeout běžnému uživateli; server dál zakazuje timeout staff rolí. Odebrán přístup MOD k bodům, odměnám, objednávkám, hrám a predikcím a v UI jsou skryté ban, ticket, blok sázek, export i úprava bodů. Přímé API obcházení role/flags/admin-meta/gamble/points je kryté regresními testy. Cílené testy 73/73 a kompletní predeploy 519/519 včetně frontend syntax/balance prošly. Bez deploye a změny produkčních dat.
- **[Codex] Turnstile konfigurace přes Chrome — čeká na přihlášení, NENASAZENO** — Provozovatel výslovně povolil použití Chrome pro vytvoření Cloudflare Turnstile widgetu. Přihlášená Cloudflare session však v Chrome není; dashboard skončil na oficiální přihlašovací stránce, která byla ponechána otevřená k bezpečnému ručnímu přihlášení/2FA. Heslo ani autentizační údaje nebyly čteny nebo přenášeny. Připravený izolovaný produkční kandidát zůstává beze změny; bez deploye a změny produkčních dat.
- **[Codex] Upřesnění pravidel Patrona sezóny — lokálně, rozhodnutí** — Potvrzeno podle aktuální implementace: 5 gift subů v kalendářním měsíci odemyká +1 patron slot pouze do konce daného měsíce a pro zachování bonusu je další měsíc potřeba znovu dosáhnout 5 giftů. Zvíře po skončení bonusu nezmizí, zůstává viditelné a funkční; pouze nelze koupit další zvíře, dokud počet obsazených míst neklesne pod aktuální počet aktivních slotů nebo se patron slot znovu neodemkne. Sloty ze stodoly jsou trvalé. Bez změny kódu, deploye a produkčních dat.
- **[Codex] Produkční kandidát třívrstvé ochrany farmy — připraven, deploy BLOKOVÁN konfigurací** — Z přesné kopie aktivního release `webos-1784718508979647100` vznikl izolovaný kandidát obsahující jen rate limit, behaviorální risk skóre a Turnstile checkpoint; nesouvisející lokální WIP kolem darů, banů, DDoS prahu, crew wars a cache byl vyloučen. Cílené testy 35/35 a izolovaný predeploy 499/499 včetně frontend syntax/balance prošly. Produkční `/opt/webos/env` však nemá Turnstile klíče a dostupný Cloudflare token vrací pro Turnstile API HTTP 403, proto nebyl spuštěn neúplný/fail-open deploy; k dokončení je potřeba výslovné povolení použít přihlášený Chrome pro vytvoření widgetu nebo dodat `site key` + `secret key`. Bez deploye a změny produkčních dat.
- **[Codex] Třívrstvá ochrana Zahrádky a Statku proti skriptům — lokálně, NENASAZENO** — Všechny mutační garden/farm endpointy sdílejí per-user limit 30 akcí/min, takže skript limit neobejde rotací cest. Stávající audit sklizní nově používá stejné vynucované skóre jako admin přehled: opakované reakce do 5 s a provoz bez spánkové pauzy jsou hlavní signály, 3+ dalších účtů na stejné IP přidá 20 bodů pouze k už existujícímu podezření a samotná sdílená IP nikdy nikoho neoznačí. Při skóre 50+ se před atomickou změnou záhonu/zvířete zastaví sklizeň či sběr a zobrazí Cloudflare Turnstile; úspěšné serverové Siteverify ověření s kontrolou `action` + hostname odemkne odměny na 24 h. Přidána tabulka `automation_verifications`, 5 pokusů o challenge/10 min, lazy frontend widget a přesné CSP `script-src`/`frame-src`; bez nakonfigurovaných `WEBOS_TURNSTILE_SITE_KEY` + `WEBOS_TURNSTILE_SECRET_KEY` je challenge fail-open a běžná farma se nemění. Regrese ověřuje, že IP sama nestačí, špatná action selže, správný token odemkne a 31. farm akce/min vrátí 429; cíleně 35/35 a kompletní predeploy 513/513 včetně JS syntax/balance prošel. In-app Browser načetl první lokální stránku, ale interakce i nový tab timeoutovaly, proto vizuální modal není označen za ověřený; izolovaný QA server/DB byly odstraněny. Bez deploye a změny produkčních dat.
- **[Codex] Návrh ochrany Zahrádky a Statku proti skriptům — lokálně, rozhodnutí** — Server už atomicky hlídá dozrání a dvojí vyzvednutí a produkce zapisuje reakční časy do `automation_events`, ale vyhodnocení je pouze auditní a mutační garden/farm endpointy nemají vlastní per-user rate limit. Doporučen nejmenší bezpečný zásah: sdílený per-user limit pro herní změny, rizikové skóre z opakovaných reakcí do 5 s + provozu bez delší pauzy a při vysokém skóre dočasně pozastavit pouze výnosy Zahrádky/Statku do jednorázové Turnstile kontroly; neudělovat automatický account/IP ban, protože hromadná tlačítka, notifikace a sdílené IP vytvářejí falešné pozitivy. Pro osm účtů z jedné IP přičítat multi-account/IP jen jako podpůrný signál. Bez změny kódu, deploye nebo produkčních dat.
- **[Codex] Ověření skriptů účtu `skytlix` — produkce diagnostika, bez zásahu** — Účet ID 631 není zabanovaný ani v timeoutu. Webová session byla naposledy aktivní v 09:01 UTC a známé IP účtu nebyly při kontrole v Top 20 provozu; audit farmy/zahrádky má 27 událostí s nejrychlejší reakcí 182,6 s, tedy bez rychlého automation signálu, a shodné časy více odměn odpovídají nativním hromadným akcím. Kick chat naopak vykazuje pattern odpovídající autosendu: 22 započtených zpráv mezi 09:56–11:25 UTC, rozestupy 180,4–346,9 s (průměr 254,6 s). Produkce neukládá odesílatele každého chat webhooku, takže počet zpráv uvnitř 180s cooldownu nelze zpětně doložit; podle doložené kadence jde přibližně o 0,24 zprávy/min proti celkovým 38,4 webhooku/min, tedy méně než 1 % a ne zdroj zpomalení webu. Skutečný aktuální top zdroj měl 164 req/min a patřil sdílenému IPv6 clusteru osmi jiných účtů. Bez banu, deploye a změny herních/uživatelských dat; načtení read-only admin traffic přehledu mohlo provést pouze běžný throttleovaný `last_seen` touch existující admin session.
- **[Codex] Ochrana webu před pollingovou špičkou — NASAZENO (cache 2026071290, release `webos-1784718508979647100`)** — Z přesné kopie aktivní produkce nasazeny pouze `app/main.py` a `web/app.js`: souběžné stejné GETy v jedné kartě se slučují, neinteraktivní globální polling se zpomalil přibližně na polovinu a jedna IP může provést nejvýše 120 pasivních obnov/min; uživatelské akce, hry, nákupy, heartbeat a aktivní aukční 30–45s refresh limitovány nejsou. Uvicorn concurrency strop na Contabo zvýšen 256→512 (záloha override `/etc/systemd/system/webos.service.d/override.conf.pre-perf-20260722`) a instalační skript lokálně sjednocen na 512. Izolovaný predeploy 505 passed / 5 explicitně deselected nesouvisejících WIP testů, cíleně 3/3 a JS syntax/balance prošly. Po deployi veřejný JS obsahuje všechny kontrakty, release soubory jsou SHA-256 shodné s kandidátem, DB `quick_check=ok`, health/maintenance OK/off, `TasksCurrent` kleslo 51→25 a worker z přibližně 110 % na 50–85 % CPU. Od restartu bylo obslouženo 7 027× HTTP 200, nadbytečný polling skončil levně 254× HTTP 429, Kick webhook 165/165× HTTP 200 a nebyla žádná 500/503, concurrency chyba, traceback ani restart; zahřátá homepage odpovídala přibližně 0,06–0,38 s. Největší zdroj `ParekvR0hliku` klesl 184→102 req/min; další aktuálně silná sdílená IP patřila `kaja32` + `kajaaa1` (107/min). Bez ručního banu a změny herních/uživatelských produkčních dat.
- **[Codex] Prověření pomalého webu během live — produkce read-only, rozhodnutí bez změny kódu** — Contabo je zdravé (10 GiB RAM dostupných, disk 12 %, SQLite `quick_check=ok`, WAL malý, služby bez pádu), ale jediný Uvicorn worker při live obsluhoval 7 138 požadavků za 5 minut, se špičkou 1 717/min a průměrem kolem 110 % CPU. Ve 12:52 CEST dosáhl nastaveného limitu 256 souběžných spojení a odmítl 17 běžných API požadavků HTTP 503, což vysvětluje dlouhé načítání. Kick webhook retry storm se neopakuje: 145 webhooků za 5 minut skončilo 145× HTTP 200. Provoz odpovídal přibližně 50 aktivním účtům za 2 minuty / 89 za 5 minut a běžným globálním polling intervalům; nadbytečné multi-tab heartbeaty byly omezeny 429. Po odeznění špičky veřejná homepage odpovídala 0,06–0,23 s a kontrolované API 0,07–0,38 s. Bez restartu, deploye nebo změny produkčních dat; doporučená následná oprava je snížit globální polling a teprve podle zátěžového ověření upravit worker/concurrency konfiguraci.
- **[Codex] Zvýšení dnešního chat cíle z 3 500 na 4 500 — prod-data** — Produkční `cgoal_target` byl na Contabo atomicky změněn na 4 500 bez resetu průběhu a bez změny odměny 1 000 sedláků. Před zásahem vznikla konzistentní záloha `/data/backups/app-before-cgoal-target-4500-20260722T105652Z.db` s původním cílem 3 500 a `integrity_check=ok`; auditní záznam #33909 je veden pod `Codex`. Veřejné API následně vrátilo cíl 4 500, DB health `ok` a maintenance off. Bez deploye.

## 2026-07-21

- **[Codex] Admin může psát do obchodního chatu při otevřeném sporu — NASAZENO (cache 2026071289, release `webos-1784664472040569500`)** — Z čerstvé kopie aktivního produkčního release 1288 nasazeny přesně 2 runtime soubory; ostatní lokální WIP byl z payloadu vyloučen. Admin/broadcaster nyní může psát pouze ve stavu `disputed` (`Řeší admin`), jeho zpráva je v chatu označena `🛡️ Admin` a notifikaci dostanou prodávající i kupující; mimo spor zůstává audit jen ke čtení. Izolovaný predeploy 501/501 + JS syntax/balance prošel; atomický restart, veřejný health `app/db=ok`, maintenance off, cache 1289 a Cloudflare purge jsou OK. Oba release soubory jsou SHA-256 shodné, veřejný JS obsahuje admin označení a service error log je prázdný. Produkční obchod #28 zůstává ve stavu `disputed`, bez změny zpráv, sedláků nebo jiných dat.
- **[Codex] Admin může psát do obchodního chatu při otevřeném sporu — lokálně, NENASAZENO** — Admin/broadcaster zůstává u běžného obchodu pouze auditorem ke čtení, ale ve stavu `disputed` (`Řeší admin`) dostane composer a může komunikovat s prodávajícím i kupujícím. Adminova zpráva se uloží do stejného auditovatelného chatu, oběma účastníkům odešle notifikaci a v UI je označena `🛡️ Admin`; po vyřešení sporu se zápis adminovi znovu uzamkne. Cílený regresní flow ověřuje zákaz před sporem, povolení po otevření sporu a viditelnost zprávy oběma stranám; 21/21 a kompletní `predeploy.py` 508/508 včetně JS syntax/balance prošly. Bez deploye a změny produkčních dat.
- **[Codex] Timeout 12 hodin s povinným důvodem — NASAZENO (cache 2026071288, release `webos-1784651922467576600`)** — Z přesné kopie aktivní produkce byl vytvořen izolovaný kandidát a nasazeno pouze 5 runtime souborů pro timeout: model/API, idempotentní migrace `users.timeout_reason`, veřejná data a frontend. Admin nabídka nyní obsahuje `Timeout 12 h`, u každého nového timeoutu vyžaduje důvod (max. 200 znaků), ukládá jej odděleně od permanentního banu, předává jej Kick API a zobrazuje hráči na společné celostránkové blokovací obrazovce; zrušení timeoutu smaže konec i důvod. Izolované testy 10/10, frontend syntax/balance a dry-run prošly; atomický restart, veřejný health `app/db=ok`, maintenance off, cache 1288 a Cloudflare purge jsou OK. Veřejný JS obsahuje volbu, prompt i obrazovku důvodu, všech 5 release souborů je SHA-256 shodných s kandidátem a service error log je prázdný. Devět dříve upravených timeoutů zůstalo beze změny do 22. 7. 06:16 CEST s důvodem `autosend po streamu`.
- **[Codex] Unlimited gamba pro účet ID 1921 (`detroyhaha`) — prod-data** — Účet byl přidán do produkčních výjimek `eco_wager_exempt_uids` i `eco_games_exempt_uids`, čímž se zrušil globální denní strop sázek i denní strop čistého zisku z her. Osobní `wager_limit` byl změněn z 150 000 na 0 a čekající navýšení 1 500 000 bylo smazáno, aby se limit další den neobnovil. Před zásahem vznikla konzistentní záloha `/data/backups/app-before-unlimited-gamba-1921-20260721T162726Z.db` s `integrity_check=ok`. Bez deploye; zůstávají běžné limity jednotlivých her (např. maximální sázka duelu).
- **[Codex] Zkrácení devíti 24h timeoutů na 12 hodin s důvodem — prod-data** — Na výslovný pokyn byly účtům `BananOS_57`, `quixikk`, `ZFilipZ57`, `FuZe52`, `CibyXx_OPK`, `filip158746`, `F1LXe`, `VulcanSK` a `cactusBoy1616` atomicky nastaveny webové timeouty do 22. 7. 06:16 CEST a důvod `autosend po streamu`. Produkční DB dostala chybějící sloupec `users.timeout_reason`; před změnou vznikla konzistentní záloha `/data/backups/app-before-timeout-12h-20260721T161605Z.db`. Do `admin_audit` bylo přidáno 9 transparentně označených záznamů pod `Interaty`. Kick timeout byl všem devíti obnoven na 720 minut se stejným důvodem, API potvrdilo 9/9 bez fallbacku. Následná read-only kontrola potvrdila shodný konec a důvod u 9/9, audit 9/9 a veřejný health `app/db=ok`, maintenance off. Bez deploye aplikace.
- **[Codex] Kontrola timeoutů udělených od 17:30 CEST — produkce read-only** — Audit na Contabo ukázal od 17:30 do času kontroly stejných 9 timeoutů od účtu `Interaty`, všechny na 24 hodin: `BananOS_57`, `quixikk`, `ZFilipZ57`, `FuZe52`, `CibyXx_OPK`, `filip158746`, `F1LXe`, `VulcanSK` a `cactusBoy1616`; byly uděleny mezi 18:00 a 18:07 CEST a nebyl zaznamenán žádný timeout ani jeho zrušení mezi 17:30 a 18:00. Bez změny timeoutů, deploye nebo produkčních dat.
- **[Codex] Timeout 12 hodin s povinným důvodem — lokálně, NENASAZENO** — Administrační nabídka timeoutu nově obsahuje `Timeout 12 h`; při udělení libovolného timeoutu musí moderátor zadat důvod (max. 200 znaků). Důvod se ukládá odděleně od důvodu permanentního banu, předává se také Kick timeoutu a uživateli se bezpečně zobrazí na společné celostránkové blokovací obrazovce. Zrušení timeoutu smaže konec i důvod. Přidána databázová migrace `users.timeout_reason` a regresní testy pro 12h délku, povinný důvod, jeho zobrazení a smazání. Cíleně 10/10 a kompletní `predeploy.py` 508/508 včetně JS syntax/balance; bez deploye a změny produkčních dat.
- **[Codex] Admin Trhu: aukce bez příhozu už nenabízí falešné escrow akce — NASAZENO (cache 2026071287, release `webos-1784649674829049800`)** — Z hashově shodného aktivního produkčního baseline nasazeny přesně 2 runtime soubory; lokální anti-DDoS a ostatní WIP byly vyloučeny. Ukončený listing je nově `vydraženo` jen při skutečném vítězi, jinak se zobrazuje `⚪ nevydraženo`, nemá Chat/Vyplatit/Refund a nabízí pouze Smazat. Přímé API volání bez kupujícího vrací přesnou hlášku, že není co refundovat ani vyplácet. Izolovaný predeploy 501/501 + JS syntax/balance prošel; atomický restart, veřejný health `app/db=ok`, maintenance off, cache 1287 a Cloudflare purge jsou OK. Oba release soubory jsou SHA-256 shodné, veřejný JS obsahuje opravu a service error log je prázdný. Produkční záznamy #13 a #15 zůstaly beze změny (`ended`, 0 příhozů, bez vítěze); žádné body ani data nebyly upraveny.
- **[Codex] Admin Trhu: aukce bez příhozu už nenabízí falešné escrow akce — lokálně, NENASAZENO** — Produkční read-only kontrola potvrdila u `AK-47 | Wintergreen (MW)` (#13) a `M4A4 | Spider Lily MW` (#15) stav `ended`, cenu/příhozy 0, `winner_id=NULL` a žádné delivery escrow; nikomu tedy nebylo co refundovat. Root cause byl pouze frontend: každý ukončený komunitní listing považoval za prodaný. Admin UI nyní vyžaduje skutečného vítěze, bezvýherní nabídku označí `⚪ nevydraženo`, skryje Chat/Vyplatit/Refund a ponechá pouze Smazat. Backend pro případ přímého volání vrací přesnou hlášku „Aukce skončila bez kupujícího – není co refundovat ani vyplácet.“ Regresní test vytváří skončenou market aukci bez příhozu a ověřuje admin data, API guard i UI kontrakt; cíleně 21/21 a kompletní `predeploy.py` 508/508 včetně JS syntax/balance. Bez deploye a změny produkčních dat.
- **[Codex] Zvýšení prahu anti-DDoS auto-banu na 1 000 req/min — lokálně, NENASAZENO** — Sdílený zdroj `ddos.AUTOBAN_PER_MIN` změněn z 500 na 1 000 požadavků za posledních 60 sekund; hodinový dočasný ban se tedy spustí až při překročení 1 000/min. Záložní hodnota v admin přehledu provozu sjednocena na 1 000 a regresní očekávání upraveno. Cílený test 1/1, `node --check` a `git diff --check` prošly. Bez deploye a změny produkčních dat.
- **[Codex] Trust Factor pro prodejce a kupující na Komunitním Trhu — NASAZENO (cache 2026071286, release `webos-1784642767900856500`)** — Z hashově shodného kandidáta aktivní produkce nasazeny přesně 3 runtime soubory: market API, frontend a styly; ostatní lokální WIP zůstal mimo payload. Karty nabídek, přehled obchodů i obchodní chat zobrazují reputaci `Nový / Ověřený / Důvěryhodný / Veterán` podle počtu úspěšně dokončených escrow obchodů zvlášť pro prodeje a nákupy, včetně upozornění, že jde o orientační údaj, nikoliv záruku. Izolovaný predeploy 500/500 + JS syntax/balance prošel; atomický restart, veřejný health `app/db=ok`, maintenance off, cache 1286 a Cloudflare purge jsou OK. Veřejný asset i API Trust data ověřeny, všechny 3 release soubory jsou SHA-256 shodné s kandidátem a service error log je prázdný. Bez migrace a změny produkčních dat nebo sedláků.
- **[Codex] Trust Factor pro prodejce a kupující na Komunitním Trhu — lokálně, NENASAZENO** — Karty nabídek, soukromý přehled obchodů a hlavička obchodního chatu nově ukazují transparentní reputaci obou stran podle počtu úspěšně dokončených escrow obchodů: `Nový` (0), `Ověřený` (1–2), `Důvěryhodný` (3–9), `Veterán` (10+). Veřejně se hodnotí prodejní historie prodejce, po uzavření obchodu se prodávajícímu ukáže nákupní historie výherce a kupujícímu prodejní historie prodejce. UI výslovně říká, že Trust Factor je orientační a není záruka; spory ani refundy automaticky neoznačují uživatele za podvodníka. Ověřeno API flow včetně změny obou účastníků z 0 na 1 po potvrzení escrow, cíleně 20/20 a kompletním `predeploy.py` 507/507 včetně JS syntax/balance. In-app Browser načetl lokální Shop bez console chyb, ale při kliknutí na Trh zavřel kartu a opakovaná čistá karta timeoutovala při navigaci; bez povoleného fallbacku nebyl pořízen screenshot. Bez deploye a změny produkčních dat.
- **[Codex] Komunitní Trh otevřen všem + escrow/float/filtry/reputace — NASAZENO (cache 2026071285, release `webos-1784638989588478200`)** — Z čisté kopie aktivní produkce nasazeno přesně 7 souborů Trhu; ostatní rozpracované změny v hlavním stromu byly z payloadu vyloučeny. Odstraněna produkční obrazovka „Soon“ i serverová 503 blokace odeslání nabídky. Živý Trh nyní nabízí pevnou cenu/aukci, veřejný popis, přesný float s automatickým wear, hledání a filtry, reputaci prodejce a potvrzované escrow: prodávající označí odeslání, kupující převzetí, teprve poté se vyplatí 95 %; spor řeší admin výplatou nebo refundem. Izolovaný predeploy 500/500, frontend syntax/balance a diff kontrola prošly; atomický restart, health `app/db=ok`, maintenance off a Cloudflare purge jsou OK. Produkční migrace přidala všech 9 aukčních + 2 submission sloupce; 13 historicky vyplacených obchodů označila jako dokončené, 30 aktivních nabídek a 2 čekající návrhy zachovala. Veřejný JS neobsahuje žádný market gate, `/api/auctions` odpovídá a service error log je prázdný.
- **[Codex] Komunitní Trh: potvrzované escrow, float, stavy, filtry a reputace — lokálně, NENASAZENO** — Okamžitá výplata prodávajícímu byla nahrazena skutečným escrow tokem: po nákupu zůstane cena zamčená, prodávající označí skin jako odeslaný a teprve potvrzení kupujícího připíše 95 % prodávajícímu a spálí 5 %. Obě strany mohou otevřít spor; admin jej z panelu nevratně vyřeší výplatou nebo refundem ceny včetně vítězova vstupního aukčního poplatku. Nevyřízené escrow nejde smazat a staré už vyplacené obchody migrace označí jako dokončené. Nabídka nově přijímá přesný float 0–1, automaticky z něj určí `FN/MW/FT/WW/BS` na správných hranicích a zobrazuje float veřejně i adminovi. Trh má hledání, filtr pevná cena/aukce, wear a cenové rozmezí; karta ukazuje počet dokončených obchodů prodejce a soukromý přehled jasný stav `čeká/odesláno/spor/dokončeno/refundováno`. Ověřeno cíleně 20/20, kompletním `predeploy.py` 507/507, JS/Python syntax, frontend balance, `git diff --check` a lokálním API nad dočasnou DB. In-app Browser načetl lokální Shop, ale jeho CDP při kliknutí na Trh timeoutoval; bez nepovoleného browser fallbacku nebyl pořízen nový screenshot. Bez deploye a změny produkčních dat.
- **[Codex] Trh: veřejný popis a přesný stav opotřebení skinu — lokálně, NENASAZENO** — Opraven datový tok `nabídka → schválení → veřejná karta`: popis se nově uloží i do vzniklé komunitní aukce, vrací ho veřejné API a karta ho bezpečně escapovaně zobrazí; existující schválené nabídky se při migraci doplní z původního submission záznamu. Formulář má samostatný výběr `FN / MW / FT / WW / BS` včetně float intervalů, veřejná karta i admin fronta zobrazují čitelný štítek a backend odmítá neplatné hodnoty. Ověřeno cíleně 18/18 a kompletním `predeploy.py` 505/505 včetně frontend balance a JS syntax, `git diff --check` i lokálním API nad dočasnou DB; in-app Browser načetl Shop, ale při přechodu na Trh se jeho tab zavřel a nový lokální náhled timeoutoval, proto bez nového vizuálního screenshotu. Bez deploye a změny produkčních dat.
- **[Codex] Odstranění hrubého otisku z Anti-automation přehledu — NASAZENO (cache 2026071284, release `webos-1784598528883666300`)** — Z izolované kopie aktivní produkce nasazeno pouze zrušení fingerprint vazeb v novém automation reportu a odpovídající čtyři frontendové řádky; lokální market/crew WIP nebyl součástí release. Přehled nyní propojuje účty jen při shodě přihlašovací IP, sloupec nese „Účty na stejné IP“ a IP sama skóre nezvyšuje. Izolovaný predeploy 496/496 + frontend syntax/balance prošel; atomický restart, health `app/db=ok`, maintenance off a Cloudflare purge jsou OK. Veřejná cache 1284 je SHA-256 shodná s release, obsahuje pouze označení `(IP)`, stará fingerprint větev chybí, čtyři kontrolované release soubory jsou hashově shodné a service error log je prázdný. Bez změny uživatelských stavů a produkčních dat.
- **[Codex] Odstranění hrubého otisku z Anti-automation přehledu — lokálně, NENASAZENO** — Nový auditní report už nepropojuje účty podle slabého client fingerprintu, který na běžných mobilech vytvářel dlouhé falešné seznamy; zobrazuje jen účty se skutečnou shodou přihlašovací IP, stále pouze informačně a bez vlivu na skóre. Frontend odstranil označení „(otisk)“, sloupec se jmenuje „Účty na stejné IP“ a vysvětluje, že IP sama skóre nezvyšuje. Ostatní existující bezpečnostní použití fingerprintu nebylo měněno. Regresní test potvrzuje, že shodný otisk bez shodné IP nevytvoří vazbu; cíleně 2/2, `node --check` a `git diff --check` prošly. Bez deploye a změny produkčních dat.
- **[Codex] Auditní anti-automation přehled pro zahrádku a statek — NASAZENO (cache 2026071283, release `webos-1784593626758565600`)** — Z izolované kopie aktivní produkce nasazeny pouze auditní události zahrádky/statku, vysvětlitelné skóre a admin přehled; lokální rozpracované změny trhu, crew cooldownu a starší cache registrace byly z payloadu výslovně vyloučeny. Izolovaný predeploy 496/496 + frontend syntax/balance prošel, atomický restart, health `app/db=ok`, maintenance off a Cloudflare purge jsou OK. Produkční cache je 1283, endpoint `/api/admin/security/automation` bez relace vrací 401, tabulka `automation_events` vznikla prázdná (0/0 bez backfillu), osm release souborů je SHA-256 shodných se stagingem a service error log po deployi je prázdný. Režim zůstává AUDIT ONLY bez automatických banů, timeoutů nebo snížení odměn; produkční data se mění jen novými auditními záznamy při budoucích sklizních.
- **[Codex] Auditní anti-automation přehled pro zahrádku a statek — lokálně, NENASAZENO** — Úspěšné serverové sklizně zahrádky a statku nově ukládají přesný čas dozrání, vyzvednutí a reakční latenci; jednotky z jednoho bulk kliknutí se v reportu slučují do jednoho batch. Admin → Bezpečnost → Anticheat má nový přehled „Anti-automation“ se skóre 0–100, konkrétními důvody, posledními reakcemi, nejdelší pauzou a informačními vazbami účtů přes IP/otisk. Režim je pouze AUDIT ONLY: nikoho automaticky nebanuje, netimeoutuje ani nesnižuje odměny a IP/fingerprint samy skóre nezvyšují. Endpoint /api/admin/security/automation je striktně jen pro admina. Ověřeno cíleně 32/32, admin API nad testovacími daty (skriptový vzor 80/100, lidský 0/100), veřejný frontend bez console error a celý predeploy.py 504/504 + frontend syntax/balance. Přihlášený admin render nebylo možné v in-app browseru vizuálně otevřít bez nepovoleného vložení testovací cookie; API oprávnění a zapojení rendereru kryje regresní test. Bez deploye a bez změny produkčních dat.
- **[Codex] Odstranění homepage panelu „Rychlé akce“ — NASAZENO (cache 2026071282, release `webos-1784592713396693200`)** — Z izolované kopie aktivní produkce nasazeno pouze odstranění homepage panelu se zkratkami pro hromadnou sklizeň, sběr/krmení statku, bonusy a úkoly; běžné akce na příslušných stránkách zůstaly. Payload se před cache bumpem lišil přesně v `web/app.js` a `web/styles.css`; predeploy 494/494, frontend balance a JS syntax prošly. Atomický restart, health `db=ok/maintenance=off`, cache 1282 a Cloudflare purge ověřeny; veřejné JS/CSS už neobsahují quick-action renderer ani styly. Bez změny produkčních dat.
- **[Codex] Doporučený postup anticheatu — lokálně, rozhodnutí bez změny kódu** — Preferována dvoufázová ochrana před plošným denním limitem: nejprve 3–7 dní audit-only serverový sběr reakčních latencí, no-sleep oken a opakovaných multiaccount sekvencí s důkazy v administraci; poté Turnstile pouze pro vysoké risk score, s rozpadem skóre po úspěšném ověření a bez automatického permanentního banu. Tím zůstane běžné hraní i supporter výhody beze změny a známý 24/7 cluster bude vyzván k ověření. IP/fingerprint jen podpůrný signál, nikdy samostatný důvod; plošná CAPTCHA a hard cap nedoporučeny jako první krok. Měkké snížení výnosu přidat až tehdy, pokud se skripty naučí lidské prodlevy a challenge obcházet. Bez implementace, deploye nebo změny produkčních dat.
- **[Codex] Odstranění homepage panelu „Rychlé akce“ — lokálně, NENASAZENO** — Z homepage odstraněn celý panel se zkratkami pro hromadnou sklizeň, sběr/krmení statku, bonusy a úkoly včetně mrtvého rendereru a CSS. Standardní akce přímo v Zahradě, Statku, Bonusech a Úkolech zůstaly beze změny. Ověřeno `node --check`, kontrolou nulových referencí na quick panel a `git diff --check`. Bez deploye a změny produkčních dat.
- **[Codex] Návrh serverového anticheatu proti automatizaci — lokálně, rozhodnutí bez změny kódu** — Doporučena evidence-first V1 v centrálních zahradních/statkových award cestách: zaznamenat user/action, serverový ready time, skutečnou reakční latenci a batch, počítat risk score z opakovaných reakcí do několika sekund, provozu bez spánkové pauzy a sekvenčního střídání více účtů; samotná IP, slabý fingerprint ani webdriver nesmí stačit. První práh pouze označí účet v administraci s konkrétními důkazy, vyšší práh pozastaví danou odměnu a vyžádá Cloudflare Turnstile, opakované selhání jde k ručnímu timeoutu/banu — žádný automatický permanentní ban. Ověřena aktuální oficiální pravidla: Turnstile Free je použitelný, token musí backend povinně ověřit přes Siteverify, je jednorázový a platí 5 minut; Enterprise Ephemeral IDs nejsou pro V1 nutné. Sofistikovaný jednoúčtový skript s lidskými prodlevami nelze spolehlivě odlišit bez challenge nebo ekonomického limitu. Bez implementace, deploye nebo změny produkčních dat.
- **[Codex] Vysvětlení IP překryvu `op_domy` / `skytlix` — produkce read-only, rozhodnutí bez změny kódu** — Překryv tvoří jediná historická veřejná IP: `op_domy` se z ní přihlásil přes Kick OAuth 25. 6. v 20:40 lokálně a `skytlix` 26. 6. v 11:40, tedy přibližně o 15 hodin později. Oba user-agenty uváděly běžný Android Chrome, ale nemají žádný společný fingerprint. Jedna stará session z této IP zůstává u obou stále platná, session však uchovává IP při svém založení a nedokazuje, že jsou na stejné síti právě teď; jejich nejnovější přihlašovací IP se neshodují. Samotný jednorázový překryv může vzniknout domácí Wi‑Fi, mobilním CGNAT, veřejnou sítí nebo VPN a není důkazem stejného člověka, zařízení ani automatizace. Bez banu, timeoutu, deploye nebo změny produkčních dat.
- **[Codex] Hloubková kontrola mrkvových cyklů `op_domy` — produkce read-only, rozhodnutí bez změny kódu** — Historické výsadby, hnojení a sklizně byly spárovány s přesným serverovým časem dozrání (hnojivo půlí zbývající dobu). U 12 dohledatelných pohnojených mrkvových cyklů reagoval účet po dozrání s velmi proměnlivým zpožděním: 4,5 s až 35 min 56 s, medián přibližně 2 min 46 s; pouze 2/12 sklizní byly do 15 s a 3/12 do 60 s. Posledních pět reakcí bylo přibližně 8:10, 0:52, 6:30, 3:30 a 2:05 po dozrání. Tři pohnojené bramborové cykly byly sklizeny až 15,9–40,7 min po dozrání. Pro srovnání potvrzený automatizační vzor `RidicNakladaku` u posledních tří plných bramborových cyklů reagoval 1,9 s, 1,1 s a 0,6 s po serverovém dozrání. Proměnlivost `op_domy`, dlouhé denní pauzy, absence noční zahrádky, nulový webdriver/automation UA a žádný sdílený fingerprint odpovídají ručnímu hraní s budíkem/push notifikací; automatizaci nelze absolutně vyloučit, ale data ji nepodporují. Bez banu, timeoutu, deploye nebo změny produkčních dat.
- **[Codex] Čerstvá kontrola automatizace `MiSaK750` a `op_domy` — produkce read-only, rozhodnutí bez změny kódu** — Kontrola v 00:53 lokálního času nenašla nový podezřelý vzor. `MiSaK750`: za osm dní 4 zahradní a 7 farmářských batchů, žádná aktivita 01–05, maximální pauza téměř 120 h, poslední zahrádka 20:42 a statek 23:50; dvě IP/fingerprinty bez vazby na další účty, `webdriver=0`. `op_domy`: 32 zahradních a 43 farmářských batchů, žádná noční zahrádka, dvě noční farmářské akce, pauzy až 17,66/13,02 h; poslední aktivita 22:02. Poslední mrkvová série měla lidsky proměnlivé přibližně 31–38min rozestupy a poté skončila. Jediná sdílená přihlašovací IP se `skytlix`, ale žádný sdílený fingerprint ani webdriver signál. U obou bez dostatečné indicie automatizace; bez banu, timeoutu, deploye nebo změny produkčních dat.
- **[Codex] Alternativy ochrany zahrádky/statku před automatizací — lokálně, rozhodnutí bez změny kódu** — Vedle denního profit capu navrženy varianty, které odstraňují potřebu budíků: serverový sklad produktů na 12–24 hodin a automatické krmení z vlastní zásoby; delší plodiny a denní zakázky místo půlhodinového opakování; omezený zahradník/queue dostupný všem; odměna za první rozmanité denní aktivity místo počtu cyklů; risk-based human challenge pouze pro 24/7 přesné multiaccount vzory. Nedoporučeno plošné CAPTCHA, náhodné tresty ani zákaz podle IP/fingerprint kvůli obtěžování poctivých hráčů a false positives. Preferovaná kombinace: 12hodinový sklad statku + denní zahradní zakázky/delší cykly + challenge jen pro vysoce rizikové účty; sub perk jako větší sklad/pohodlí, ne nutnost grindit déle. Bez deploye nebo změny produkčních dat.
- **[Codex] Návrh ochrany zábavního grindu před automatizací — produkce read-only, rozhodnutí bez změny kódu** — Produkční rozdělení za posledních sedm dní ukazuje u zahrádky medián 8 a 95. percentil 28 sklizených políček na účet/den; u statku medián 7 a 95. percentil 22 produktů. Doporučen serverový denní „fun budget“ přibližně 30 sklizní + 24 produktů: do limitu plný výnos, po limitu výrazně snížený výnos a nulové XP, počítaný podle skutečných jednotek v centrálních award funkcích, takže jej nelze obejít přepnutím mezi single/bulk endpointy. Stejný počet akcí pro všechny; sub perk zůstane ve vyšším výnosu/XP, ne v nutnosti grindit déle. Druhá vrstva: audit mutačních akcí a risk score (24/7 bez spánkové pauzy, opakované přesné maturity, více účtů na stejné vazbě), který pouze zobrazí administraci a u vysokého rizika vyžádá challenge; bez automatického banu kvůli false positives. Stávající statek má 20k měkký denní bodový strop s 25% výnosem nad něj, ale je výrazně nad 95. percentilem běžného denního provozu; zahrádka nemá grind cap vůbec. Bez deploye nebo změny produkčních dat.
- **[Codex] Prověření automatizace `MiSaK750` a `op_domy` — produkce read-only, rozhodnutí bez změny kódu** — `MiSaK750` má za osm dní pouze 4 zahradní a 7 farmářských akčních batchů, žádné akce v lokálním čase 01–05, pauzu až přibližně 120 hodin, dvě přihlašovací IP a dva fingerprinty bez vazby na jiný účet; bez webdriver/automation user-agent signálu. `op_domy` je aktivnější (32 zahradních a 43 farmářských batchů za osm dní), ale má běžné 13–18hodinové pauzy, žádnou noční zahrádku a pouze dvě noční farmářské akce. Krátké mrkvové série kolem 30 minut jsou nepravidelné, omezené na několikahodinová aktivní okna a odpovídají nativním bulk tlačítkům + hnojivu; nejde o 24/7 přesný cyklus jako u potvrzeného clusteru. Jedna přihlašovací IP se překrývá se `skytlix`, ale fingerprint se nepřekrývá s nikým. U obou bez dostatečného důvodu označit účet za automatizovaný; bez banu, timeoutu, deploye nebo změny produkčních dat.
- **[Codex] Analýza automatizace zahrádky a statku — produkce read-only, rozhodnutí bez změny kódu** — Současný web ukládá časy ekonomických změn do `points_log` a klientský `webdriver` signál, ale neauditoval přímo každý mutační endpoint; všech 113 799 dosavadních client signals má `webdriver=0`, takže userscript/automatizaci v běžném prohlížeči tento signál neodhalí. Nativní bulk tlačítka zároveň legitimně vytvářejí několik akcí během sekund. Nejsilnější současná anomálie je devítiúčtový cluster kolem `RidicNakladaku`: osm účtů sdílí přihlašovací IP a devět slabý fingerprint; `RidicNakladaku`, `BartosAdam`, `KomediantObecny`, `ParekvR0hliku`, `SousedsVrtackou` a `SpartanskySlavista` opakují fertilizované bramborové cykly přibližně po 90 minutách a po stejném pořadí účtů. Např. `BartosAdam` má 15/21 intervalů v pásmu 85–95 min, `KomediantObecny` 9/11 a `RidicNakladaku` 19/37; poslední zároveň během dvou dnů zasahuje 22 různých hodin a nemá pauzu delší než 2,74 h. Jde o vysoké podezření na jednoho operátora s multiúčty/automatizací, ne nezvratný důkaz skriptu; bez banu, timeoutu, deploye nebo změny produkčních dat.

## 2026-07-20

- **[Codex] Oprava bílé stránky po permanentním banu — NASAZENO (cache 2026071281, release `webos-1784583488189934000`)** — Z izolované kopie aktivní produkce 1280 nasazena pouze oprava zachování relace v `app/routers/admin.py` a `app/routers/auth.py`: jednotlivý ban, cluster ban ani fingerprint auto-ban už nemažou session, takže `/api/auth/me` dokáže vykreslit společnou celostránkovou ban/timeout grafiku; chráněné akce dál blokuje `require_user` HTTP 403. Izolovaný payload měl před cache bumpem přesně dva změněné soubory, predeploy 501/501 a JS syntax prošly. Atomický restart, health `app=ok/db=ok/maintenance=off`, cache 1281 a Cloudflare purge ověřeny; veřejný web 5/5 HTTP 200 za 0,078–0,087 s, `TasksCurrent=15` a 0 nových error/traceback/concurrency/500/503 záznamů. Bez změny produkčních dat; už dříve smazanou relaci `Martech1` nelze obnovit, účet se musí jednou znovu přihlásit přes Kick.
- **[Codex] Oprava bílé stránky po permanentním banu — lokálně, NENASAZENO; produkce read-only** — Produkční účet `Martech1` byl v 23:24 zabanován s důvodem `TEST`, ale ban endpoint současně smazal všechny jeho webové relace. Frontend pak přes `/api/auth/me` nedokázal zjistit, že má vykreslit celostránkový `Permanently banned` stav; veřejné HTML, JS, CSS, obrázek i CSP byly přitom dostupné bez chyby. Ban účtu, cluster ban i fingerprint auto-ban nyní relaci ponechají pouze pro identifikaci a vykreslení ban stránky; všechny chráněné akce dál blokuje `require_user` HTTP 403. Timeout už relaci nemaže a používá stejnou obrazovku. Přidán regresní test skutečného admin ban endpointu; cílených 77/77 a celá sada 502/502 prošly. Bez deploye a bez změny produkčních dat.
- **[Codex] Stejná celostránková grafika pro timeout i permanentní ban — NASAZENO (cache 2026071280, release `webos-1784582364663792000`)** — Z izolované kopie aktivní produkce 1279 nasazeno pouze rozšíření existující ban obrazovky v `web/app.js`: aktivní `timeout_until` používá stejné `ban-bg.webp` pozadí a blokuje Shop, navigaci i polling; text se přepne na `Timeout`, ukazuje živý zbývající čas a konec a po vypršení stránku automaticky obnoví. Nově udělený timeout v otevřené relaci zachytí sdílená 403 větev a načte čerstvý stav účtu. Cílené testy 9/9, izolovaný predeploy 500/500 a JS syntax prošly; atomický restart, health `app=ok/db=ok/maintenance=off`, cache 1280 a Cloudflare purge ověřeny. Veřejný JS obsahuje timeout gate, odpočet i auto-unlock, 5/5 homepage probe HTTP 200 a 0 nových traceback/error/concurrency/500/503 záznamů. Bez změny produkčních dat a stavů banů/timeoutů.
- **[Codex] Stejná celostránková grafika pro timeout i permanentní ban — lokálně, NENASAZENO** — Existující ban renderer nyní blokuje i aktivní `timeout_until`, takže timeoutovaný ne-admin nevidí Shop, navigaci ani polling a dostane stejné `ban-bg.webp` pozadí. Text se podle stavu přepne na `Timeout`, zobrazuje živý zbývající čas i konec v lokálním formátu a po vypršení stránku automaticky obnoví; permanentní ban dál ukazuje svůj důvod. Nově udělený timeout v již otevřené relaci zachytí sdílená 403 větev a načte čerstvý stav účtu. Cílené testy 9/9, JS syntax, diff check a celý predeploy 501/501 prošly. In-app Browser načetl lokální Shop, ale následné kliknutí, DOM i navigace opakovaně timeoutovaly, proto bez nepovoleného fallbacku nevznikl nový interakční screenshot. Bez deploye a produkčních dat.
- **[Codex] Celostránkový `Permanently banned` stav účtu — NASAZENO (cache 2026071279, release `webos-1784581248695988600`)** — Z izolované kopie aktivní produkce 1278 nasazena pouze nová ban obrazovka: zabanovaný ne-admin před vykreslením aplikace uvidí přes celý web `Permanently banned`, uložený důvod, kontakt na ADMINA a funkční odhlášení; při banu v otevřené relaci obrazovku aktivuje i sdílená 403 odpověď. Shop, navigace, footer, heartbeat a další polling se zabanovanému účtu nespustí. Přidáno responzivní 90kB WebP pozadí zamčené farmy a všechny účetní ban hlášky nyní odkazují na ADMINA. Izolovaný predeploy 500/500 a JS syntax prošly, atomický restart, health `app=ok/db=ok/maintenance=off`, cache 1279 i Cloudflare purge ověřeny. Veřejně ověřeny nové JS/CSS/WebP assety, 5/5 homepage odpovědí HTTP 200 a 0 nových traceback/error/concurrency záznamů. Bez změny produkčních dat a stavů banů.
- **[Codex] Celostránkový `Permanently banned` stav účtu — lokálně, NENASAZENO** — Zabanovaný ne-admin účet se po `/api/auth/me` zastaví před vykreslením aplikace: nevidí Shop, navigaci ani footer, nespustí se jeho polling/heartbeat a při banu během otevřené relace stejnou obrazovku aktivuje první sdílená 403 odpověď. Nová responzivní ZURYS obrazovka používá vygenerované a na 90 kB WebP zkomprimované pozadí zamčené farmy, zobrazuje `Permanently banned`, uložený důvod, „kontaktuj ADMINA“ a funkční „Odhlásit účet“. Všechny účetní ban hlášky nově odkazují na ADMINA místo streamera. Společný logout používá neblokující `serviceWorker.getRegistration()` místo čekání na `ready`, takže funguje i bez aktivního service workeru. Cílený test, syntax, `git diff --check` a celý predeploy 501/501 prošly. Browser ověřil title/DOM bez Shopu; kvůli opakovanému timeoutu jeho screenshotu proběhlo obrazové QA přes lokální Playwright: desktop 1672×941, mobil 390×844 bez overflow, logout a čistá konzole prošly. Bez deploye a produkčních dat.
- **[Codex] Vizuální ověření permanentního banu účtu — lokálně, rozhodnutí bez změny kódu** — Na izolované dočasné DB vytvořen účet s `banned=1` a důvodem `Permanently banned`; produkce ani reálné účty nebyly použity. Ověřeno, že současný frontend nemá samostatnou ban stránku: účet zůstane přihlášený a Shop se normálně zobrazí, ale první chráněná akce skončí HTTP 403 a červeným toastem „Tvůj účet byl zablokován (anticheat). Kontaktuj streamera.“ Pořízen screenshot skutečného stavu, DOM i interakce prošly a konzole byla bez warn/error. Bez deploye a produkčních dat.
- **[Codex] Návrh KPP engagement vrstvy pro `zurys.live` — lokálně, rozhodnutí bez změny kódu** — Ověřena aktuální oficiální pravidla Kick Partner Programu a možnosti Kick Dev API. Doporučena první verze: privátní live KPP panel (CCV, unikátní chatteři, chat rate, ticho v chatu, follow/sub události), bezpečný prompt/otázka do overlaye a chat goal založený hlavně na unikátních chatterech. Stávající webhook `chat.message.sent`, stream status, predikce a 5min chat cooldown jsou použitelný základ. Nedoporučeno odměňovat množství zpráv nebo veřejně odhadovat přesnou hodinovou sazbu: Kick zakazuje umělou manipulaci metrik a sazbu/výpočet KPP označuje za důvěrné. Současný `Top Chatters` payout podle počtu odměněných zpráv a raw message community goal je vhodné před KPP upravit, aby nemotivoval spam. Bez deploye a produkčních dat.
- **[Codex] Kontrola 4 původních banů po rollbacku — produkce read-only** — Před hromadnou akcí a po jejím přesném obnovení zůstávají zabanovaní: `miropiropo` (Majkill), `Mxtys3k` (Duplikát — sloučeno do `Ksiltovka` #980), `The_Magician2` (Nword) a `DUKLAMICHALOVCEFAN` (BANNED AC — `PojdNaCajes` ALTACC). Bez změny produkčních dat.
- **[Codex] Obnovení účtů po hromadném permanentním banu — prod-data** — Na pokyn ownera vrácen rollback batch `mass-ban-20260720-all-except-interaty`: všech 2 037 zasažených účtů dostalo přesně svůj stav před hromadnou akcí, kontrola proti snapshotu má 0 rozdílů. Zůstaly pouze 4 bany, které existovaly už před hromadnou akcí; důvod `Permanently banned` z hromadné akce nezůstal u žádného účtu. `Interaty` zůstává odemčený. V průběhu vznikl jeden nový účet mimo snapshot, ten nebyl hromadným banem zasažen. Rollback je označen `restored_at`, zapsán `admin_audit`; produkce `app=ok/db=ok/maintenance=off`.
- **[Codex] Hromadný permanentní ban všech účtů kromě `Interaty` — prod-data** — Na výslovný pokyn ownera nastaveno `banned=1`, důvod `Permanently banned`, u 2 037 z 2 038 produkčních účtů; jediný admin `Interaty` (ID 12) zůstal `banned=0`. Před změnou byl původní stav všech zasažených účtů uložen do rollback tabulky `mass_ban_restore` pod dávkou `mass-ban-20260720-all-except-interaty`; obsahuje i 4 dříve zabanované účty, takže pozdější unban této dávky obnoví jejich původní bany místo jejich chybného odemčení. Zapsán `admin_audit`. Restart `webos` vyčistil ownerův omylem chycený in-memory IP auto-ban; health `app=ok/db=ok`, služba active/running. Kontrola: 2 037 banned, 0 chybných důvodů, `Interaty` odemčený.
- **[Codex] Rozložení startovních API špiček a klidnější polling — NASAZENO (cache 2026071278, release `webos-1784564622673979500`)** — Z izolované kopie aktivní produkce 1277 nasazena pouze optimalizace klientského provozu: Shop prioritně načte produkty/drop/aukce, vedlejší panely rozloží o náhodných 0,75–2 s a odložený timer při opuštění stránky zruší; aukce, partner odkazy, cíle a feed pollují náhodně po 30–45 s, DM/notifikační badge po 45–60 s. Interaktivní hry a okamžitý refresh po vlastní akci zůstaly. Izolovaný predeploy 499/499 a frontend syntax prošly, atomický restart, health `app=ok/db=ok/maintenance=off`, cache 1278 a Cloudflare purge ověřeny. Minutu po deployi 857 requestů: 851×200, 6×304, 0×500/503 a 0 concurrency/traceback chyb; health po zahřátí 39–75 ms, `TasksCurrent=22`. Bez změny produkčních dat.
- **[Codex] Rozložení startovních API špiček a klidnější polling — lokálně, NENASAZENO; produkce read-only** — Produkce při kontrole běžela bez 500/503 (`MemoryCurrent` 169 MB, `TasksCurrent=29`), ale klienti spouštěli ve stejných sekundách velké skupiny čtecích API. Shop proto nově načte prioritně produkty, aktivní drop a aukce; vedlejší aktivitu/prodeje/milník/týdenní souhrn rozloží o náhodných 0,75–2 s a odložený timer se při opuštění či překreslení stránky zruší. Aukce, partner odkazy, komunitní/sub cíle a shop feed pollují po náhodně rozložených 30–45 s; DM/notifikační badge po 45–60 s. Interaktivní herní polling a okamžitý refresh po vlastní akci zůstaly. Cílený test, `node --check`, `git diff --check` a celý predeploy 500/500 prošly. Bez deploye a změny produkčních dat.
- **[Codex] Hodinové persistentní auto-bany proti request floodu — NASAZENO (cache 2026071277, release `webos-1784563617173640400`)** — Z izolované kopie aktivní produkce 1276 nasazena pouze tvrdší ochrana: agregovaný práh 500 req/min na IP, auto-ban 60 minut, zápis do `ip_bans` pro přežití restartu a malá JSON 403 pro další API spam; soft strop 300/min na jeden endpoint i oprava partner-flash timerů zůstaly. Neomezená údržba byla před deployem vypnuta. Lokální plný predeploy 500/500 i izolované kontrakty prošly; atomický restart, health `app=ok/db=ok/maintenance=off`, Cloudflare purge a veřejná cache 1277 ověřeny. Živý anti-DDoS endpoint potvrzuje `threshold_per_min=500`, `ban_minutes=60`; veřejný web 8/8 HTTP 200 (0,06–0,48 s). Po otevření webu 956 requestů/30 s, 902×200, 0 HTTP 500, 0 `Exceeded concurrency limit`, `TasksCurrent=33`, health 0,010 s. Bez ručního banu nebo změny uživatelských/herních produkčních dat.
- **[Codex] Tvrdší hodinové auto-bany + audit domnělého pádu po otevření webu — lokálně, NENASAZENO; produkce read-only** — Po release 1276 byl web veřejný od 17:55:36 do 17:58:19: obsloužil 4 205 requestů (3 769×200), 0 HTTP 500 a 0 `Exceeded concurrency limit`; tedy po opravě nespadl. V 17:58:19 zapnul admin `Interaty` z IP `193.179.66.72` znovu neomezenou údržbu, proto nyní veřejný `/` správně vrací 503, zatímco origin health je 200 a `TasksCurrent=24`. Na přání ownera přesto zpřísněna ochrana: agregovaný auto-ban práh 1 000→500 req/min na IP, délka 10→60 min, auto-ban se zapisuje do `ip_bans` a přežije restart; při dočasně zamčené DB zůstane aspoň in-memory ochrana. Zabanované API dostane malou JSON 403 místo plné HTML stránky, čímž další spam stojí méně CPU/přenosu. Cílené testy 7/7, syntax, `git diff --check` a celý predeploy 500/500 prošly. Bez deploye, nových banů nebo změny produkčních dat.
- **[Codex] Oprava partner-flash request stormu + soft API rate limit — NASAZENO (cache 2026071276, release `webos-1784562941701794600`)** — Z izolované kopie aktivní produkce nasazeny pouze dvě ochrany: frontend už nevytváří souběžné/osiřelé partner-flash timery a stejná IP má strop 300 volání/min na jeden API endpoint; nad limit dostane levné `429` + `Retry-After: 1` před autentizací a DB, bez trvalého zákazu skriptů. Před deployem byla nalezena neomezená nouzová údržba bez záznamu v deníku, vypnuta jako nutná podmínka deploye. Izolovaný predeploy a lokální plná sada 499/499 prošly; atomický restart, health `app=ok/db=ok/maintenance=off`, Cloudflare purge a veřejná cache 1276 ověřeny. Živý kontrakt s testovací IP vrátil přesně 300×404 a 301. požadavek `429`; veřejný web 6/6 HTTP 200 (0,06–0,47 s), po obnovení provozu health 0,013 s, `TasksCurrent=28` a 0 `Exceeded concurrency limit`. Bez změny uživatelských/herních produkčních dat.
- **[Codex] Soft rate limit proti pádu webu při skriptech/request stormu — lokálně, NENASAZENO** — Skripty a multi-account provoz zůstávají povolené, ale stejná IP může volat jeden API endpoint maximálně 300×/min. Další pokusy dostanou levné `429` + `Retry-After: 1` v middleware ještě před autentizací, DB a drahou aplikační logikou; ostatní endpointy i uživatelé pokračují normálně a stávající celkový auto-ban zůstává jako druhá pojistka. Spolu s opravou osiřelých partner-flash timerů tím request storm nemá znovu zaplnit Uvicorn limit 256 spojení. Regresní test ověřuje strop, 429, hlavičku i to, že požadavek nejde dál do DDoS/app pipeline; cílené testy 7/7, syntax, `git diff --check` a celý predeploy 499/499 prošly. Bez deploye a produkčních dat.
- **[Codex] Identifikace zdrojů request stormu po partner flash kole — produkce read-only** — `6k requestů/30 s` neznamenalo 6 000 lidí, ale opakované volání API zaseknutými frontendovými timery. Živý anti-DDoS přehled sledoval 81 IP a automaticky na 10 minut zablokoval 4 IP po překročení 1 000 req/min: `161.97.162.218`, `45.38.85.87`, `104.252.65.38`, `2a02:c207:2329:1417::1`. Produkční session vazby: první IP `ParekvR0hliku`; druhá `VlhkaKaca`, `PrdelTvojiMatky`; třetí `KralBaziny`, `CeskySmoula`; IPv6 `KonomrdLesni`, `SousedsVrtackou`, `KomediantObecny`, `RidicNakladaku`, `SpartanskySlavista`, `BartosAdam`, `MajitelFelicie`, `SituaceKramne`. Jde o vazbu session na IP, ne důkaz, který konkrétní účet request odeslal; shodné user-agenty a více účtů na jedné IP odpovídají multi-account/proxy/browser-farm clusteru, ale bez důkazu úmyslného DDoS. Bez změny kódu, banů nebo produkčních dat.
- **[Codex] Oprava `503 Service Unavailable` po skončení partner flash kola — lokálně, NENASAZENO; produkce read-only** — Produkční Uvicorn s jedním workerem narazil na limit 256 současných spojení: po vypršení flash kola vznikly při pomalých/překrytých odpovědí osiřelé sekundové frontendové timery a `/api/partner-links` + `/api/me/claims` vyskočily až na cca 5 200 volání/30 s (celkem přes 6 000 requestů/30 s). Uvicorn vracel `Exceeded concurrency limit` a 503; restart v 17:32 pomohl jen dočasně, nápor pak zachytil existující auto-ban a veřejný web se vrátil na 200. Lokální fix blokuje souběžné `loadPartnerLinks`, zahodí odpověď po opuštění stránky a každý countdown ruší vlastní timer; přidán regresní frontend kontrakt. `node --check`, cílený test, `git diff --check` a celý predeploy 498/498 prošly. Bez deploye nebo změny produkčních dat.
- **[Codex] Odlehčení načítání a pollingových špiček — NASAZENO (cache 2026071275, release `webos-1784559460088801500`)** — Produkční frontend už nečeká s prvním renderem na kosmetické crew tagy, stream status je na kartě cachovaný 60 s a skryté karty nepollují stream, aukční banner, aukce ani tajného sedláka. Aukční síťový refresh 5→15 s (sekundový lokální odpočet a okamžitý refresh po vlastní akci zachovány), partner flash 5→10 s a Trh načítá `/auctions` + `/auctions/my-sales` paralelně. Predeploy 497/497, frontend syntax a regresní performance kontrakt prošly; atomický restart, health `app=ok/db=ok/maintenance=off`, živých 5 optimalizačních JS kontraktů, cache 1275 a Cloudflare Purge Everything ověřeno. Bez změny produkčních dat. Tři bezprostřední veřejné kontroly HTML po deployi měly TTFB 0,14–0,33 s; skutečné snížení dlouhodobé zátěže se projeví po načtení nové cache v klientských kartách.
- **[Codex] Odlehčení načítání a pollingových špiček — lokálně, NENASAZENO** — Ve frontendovém release kandidátovi odstraněno blokující čekání prvního renderu na kosmetické crew tagy; tagy se načtou na pozadí a jejich požadavek se už nerozběhne duplicitně. Stav streamu je cachovaný/throttlovaný na 60 s a skryté karty nepollují stream, aukční banner, aukce ani tajného sedláka. Viditelné aukce si zachovávají sekundový lokální odpočet, síťová aktualizace byla změněna 5→15 s a po vlastním příhozu/nákupu zůstává okamžitá; partner flash polling 5→10 s. Trh načítá veřejné aukce a soukromé obchody paralelně místo sériově. Přidán regresní performance kontrakt. Izolovaný release: `node --check`, `git diff --check`, cílený test a celá sada 497/497 prošly. Browser QA na oddělené DB: Shop se vykreslil za 0,31–0,63 s, správný title/URL, neprázdný DOM, bez error overlaye a console warn/error; screenshot a následný interakční pokus zůstaly `blocked`, protože Chrome i in-app Browser opakovaně timeoutly na screenshot/CDP navigaci. Bez deploye a produkčních dat.
- **[Codex] Diagnostika pomalého načítání webu — produkce read-only** — Živá kontrola ukázala zdravou DB/aplikaci a dostatek RAM, ale jediný Uvicorn worker (`--workers 1`) obsluhoval přibližně 6 409 GETů za 2 minuty (53,4/s; `TasksCurrent=62`). Největší zdroj je `/api/auctions` s 1 369 GETy/2 min kvůli globálnímu 15s a aukčnímu 5s pollingu; následují `/api/stream/status` 955 a `/api/partner-links` 585. Při špičce veřejný HTML TTFB vystoupal na 5,37 s a API na 3–4 s, zatímco statické JS/CSS z Cloudflare trvaly jen 0,06–0,07 s a přímý origin běžně 0,06–0,20 s — jde tedy o frontu/pollingovou špičku, ne velikost assetů nebo nedostatek paměti. Trh navíc načítá `/auctions` a `/auctions/my-sales` sériově, což zpoždění sčítá. Chrome QA: stránka i Trh se vykreslily bez console warn/error; bez změny kódu, deploye nebo produkčních dat.
- **[Codex] Soukromý chat k dokončenému obchodu na Trhu — NASAZENO (cache 2026071274, release `webos-1784557758960019400`)** — Prodávající a výherce každého dokončeného komunitního prodeje nyní v sekci „Tvoje obchody“ otevřou soukromý chat pro domluvu předání skinu; admin/broadcaster má pouze read-only audit a cizí či nepřihlášený účet je odmítnut. `/api/auctions/my-sales` vrací také nákupy/výhry, prodávajícímu zůstává vítězova Steam Trade URL a každá zpráva vytvoří druhé straně notifikaci. Přidána pouze nová tabulka `market_messages` (po migraci 0 zpráv), takže chat funguje automaticky i pro existující dokončené obchody; ostatní produkční data beze změny. Izolovaný predeploy 496/496 a frontend syntax prošly, atomický restart uspěl, health `app=ok/db=ok/maintenance=off`, živý 401 kontrakt, cache/UI kontrakty a Cloudflare Purge Everything ověřeny.
- **[Codex] Soukromý chat k dokončenému obchodu na Trhu — lokálně, NENASAZENO** — Přidána tabulka `market_messages` a zabezpečené `GET/POST /api/auctions/{id}/chat`. Psát mohou pouze prodávající a výherce konkrétního dokončeného komunitního prodeje; nepřihlášený dostane 401, cizí účet 403 a admin/broadcaster má pouze read-only audit. Každá zpráva vytvoří druhé straně notifikaci, limit je 10 zpráv/min a text 2 000 znaků. `/api/auctions/my-sales` nově vrací i nákupy/výhry; Trh zobrazuje oběma stranám „Tvoje obchody“ a chat modal, prodávajícímu zachovává Trade URL, admin má tlačítko Chat u dokončené komunitní aukce. Bez globálních user-to-user DM. Aktualizovány texty předání, aby netvrdily, že tým skin drží. Cíleně 16/16 a celý predeploy 497/497 včetně frontend syntax prošly; bez deploye a produkčních dat.
- **[Codex] Zprávy mezi kupujícím a prodávajícím — lokálně, rozhodnutí bez změny kódu** — Aktuální `Zprávy` nejsou user-to-user: vlákno může založit pouze tým ZURYS a divák pak odpovídá týmu. Pro Trh doporučen soukromý chat svázaný s konkrétním dokončeným prodejem, automaticky dostupný pouze prodávajícímu a výherci, s viditelným číslem obchodu a auditovatelný adminem; ne globální DM mezi všemi kvůli spamu a scamům. Bez deploye nebo produkčních dat.
- **[Codex] Trh: prodávající vidí výherce a Trade URL — NASAZENO (cache 2026071273, release `webos-1784557034917695000`)** — Přidán soukromý autentizovaný `GET /api/auctions/my-sales`: posledních 20 dokončených komunitních prodejů vrací pouze jejich prodávajícímu, včetně uživatelského/Kick jména výherce a Steam Trade URL. Trh nově zobrazuje sekci „Tvoje prodané skiny“ s cenou a přímým tlačítkem na Trade URL; notifikace budoucích prodejů obsahuje jméno výherce. Existující prodeje #16 MAC-10 a #14 Kilowatt Case se prodávajícímu zobrazí automaticky a oba výherci mají Trade URL; bez změny produkčních dat. Predeploy 496/496, frontend syntax, atomický restart, health `app=ok/db=ok/maintenance=off`, živý privátní 401 kontrakt, cache/UI kontrakty a Cloudflare Purge Everything prošly.
- **[Codex] Trh: prodávající vidí výherce a Trade URL — lokálně, NENASAZENO** — Přidán autentizovaný `GET /api/auctions/my-sales`, který vrací posledních 20 dokončených komunitních prodejů pouze jejich prodávajícímu, včetně uživatelského/Kick jména výherce a jeho Steam Trade URL. Trh přihlášenému prodávajícímu zobrazí soukromou sekci „Tvoje prodané skiny“ s cenou a tlačítkem na Trade URL; ostatní účty data nevidí. Notifikace o prodeji nově obsahuje jméno výherce. Test ověřuje dokončení prodeje, data vlastníka i prázdný výsledek cizího účtu; cíleně 16/16, `node --check` a `git diff --check` prošly. Bez deploye a produkčních dat.
- **[Codex] Trh: výrazné pravidlo proti scamům — NASAZENO (cache 2026071272, release `webos-1784555701377346800`)** — Varování „Nulová tolerance: pokus o scam nebo nepravdivé údaje znamenají permanentní ban a odebrání všech sedláků.“ je v úvodním panelu Trhu zobrazené jako samostatný kontrastní červený box s rámečkem. Pouze frontendový text/styl, bez změny banovací logiky nebo produkčních dat. Izolovaný predeploy 496/496 a frontend syntax prošly; atomický restart, health `app=ok/db=ok/maintenance=off`, živý cache/text/CSS kontrakt a Cloudflare Purge Everything ověřeny.
- **[Codex] Trh: viditelné pravidlo proti scamům — lokálně, NENASAZENO** — Do úvodního panelu komunitního Trhu přidán výrazný varovný řádek: „Nulová tolerance: pokus o scam nebo nepravdivé údaje znamenají permanentní ban a odebrání všech sedláků.“ Jde pouze o prezentaci pravidla, bez změny banovací logiky nebo produkčních dat. `node --check` a `git diff --check` prošly.
- **[Codex] Oilers `[✡🌟✡]` overall suby — produkce read-only** — Parta Oilers (ID 2) má podle produkčního `sub_xp` celkem 171 subů (855 000 XP při 5 000 XP/sub). Rozpad aktuálních členů: Martech1 64, Numero_21 45, skytlix 27, marek_1p 19, zurys1337 12, P_A_L_F_Y 3, Vojtik58 1, Krteccek 0. Součet sedí i s červencovým čítačem party 171. Bez změny kódu, deploye nebo produkčních dat.
- **[Codex] Vysvětlení krmiva ze Zahrádky — lokálně, rozhodnutí** — Ověřeno v herní logice: každá sklizená plodina bez ohledu na druh přidá právě 1 univerzální krmivo a každé běžné zvíře spotřebuje 1 krmivo na jeden výrobní cyklus; 4 mrkve i 4 Zlaté klasy tedy nakrmí 4 zvířecí cykly. Rozdíl plodin je v době růstu a výnosu sedláků, nikoli v množství krmiva. Bez změny kódu, deploye nebo produkčních dat.

## 2026-07-19

- **[Codex] Výsledek války `Interatyho Klubík` vs `MODI KOKOTI` — produkce read-only** — Válka ID 56 skončila výhrou Interatyho Klubíku: 127 204 vs 84 866 XP. Vítěz získal navíc 42 433 crew XP kořisti (50 % XP soupeře), takže během vyhodnocené války celkem přibylo 169 637 crew XP; nejde o osobní sedláky ani osobní XP členů. Bilance Interatyho Klubíku je nově 6 výher / 1 prohra. Bez změny kódu, deploye nebo produkčních dat.
- **[Codex] Komunitní Trh otevřen všem — NASAZENO (cache 2026071271, release `webos-1784491209100799000`)** — Produkční záložka Trh už nezobrazuje `SOON`: přihlášený divák může odeslat neveřejnou nabídku skinu s pevnou cenou nebo 24h aukcí, admin ji v sekci Aukce schválí či zamítne a teprve schválená nabídka se zveřejní. Komunitní prodej blokuje vlastní nákup/příhoz, prodávajícímu vyplatí právě jednou 95 % ceny a 5 % spálí jako poplatek. Ve veřejném formuláři není upload „Z PC“ ani veřejný upload endpoint; zůstává URL a hledání v katalogu skinů. Přidána tabulka `market_submissions` a 5 market sloupců aukcí; po migraci 0 nabídek, bez vytvoření obchodů nebo změny uživatelských dat. Izolovaný release zachoval aktuální odstranění coinflip rigu (na aktivním serveru 0 výskytů). Predeploy 496/496, frontend syntax, atomický restart, health `app=ok/db=ok/maintenance=off`, živý cache/API kontrakt a Cloudflare Purge Everything prošly; anonymní submission vrací 401 a `/api/auctions/upload-image` 405.
- **[Codex] Skrytý coinflip rig odstraněn — NASAZENO (cache 2026071269, release `webos-1784488924492355600`)** — Z `app/routers/games.py` smazán celý `_coinflip_rig_winner`, jeho volání a nepoužitý import `local_date`; smazány také specializované testy, které rig záměrně ověřovaly. Coinflip nyní vždy rozhoduje jediný `secure_choice(("heads", "tails"))` přes OS CSPRNG, bez konfigurační odbočky pro hráče. Kvůli cizím rozpracovaným změnám nasazen izolovaný release vytvořený z aktivní produkce, takže nic dalšího neodjelo. Cíleně 40/40 a izolovaný predeploy 494/494 prošly, atomický restart/health/DB OK, údržba vypnutá a Cloudflare Purge Everything dokončen. Aktivní release neobsahuje `coinflip_rig` ani `_coinflip_rig_winner`, SHA-256 `games.py` sedí s lokální opravou; `/data/app.db` nemá žádné `coinflip_rig_*` settingy. Bez změny produkčních dat.
- **[Codex] Coinflip „spálit prohry za 50 → raketa“ — produkce read-only** — Přesně otestována domnělá strategie na celé historii dokončených coinflipů: vysoká sázka se počítala jen tehdy, když ji stejný zakladatel vytvořil až po známých po sobě jdoucích prohrách za 50 v předchozích 30 minutách. Po 1 prohře mělo 129 následných sázek 500+ výhernost 48,06 % a čistý výsledek −24 308; po 2 prohrách 60 případů, výhernost 50,0 % a −3 031; po 3 prohrách 25 případů, výhernost 48,0 % a −484. Ani u prahu 2 000+ nevznikla výhoda. Potvrzen gambler’s fallacy: předchozí malé prohry nezvyšují šanci dalšího hodu. Bez změny kódu, deploye nebo produkčních dat.
- **[Codex] Coinflip 50: potvrzený boost PvP úkolů — produkce read-only** — Minimální coinflip za 50 nenabíjí ani nepředpovídá další hod, ale každá výhra se bez ohledu na sázku počítá do `pvp_won`: denní úkol 4 výhry vyplácí 100 sedláků, týdenní 25 výher 700 a odznak Duelista sleduje 10/50/100 výher. Při produkčním rake 0 mohou dva koordinované účty opakovat sázky 50, mezi sebou zachovat v očekávání všechny herní body a každý získat questové odměny. Dnešní data tento motiv podporují: `jozifff` vytvořil 18 dokončených coinflipů za 50, dosáhl 6 výher a denní odměnu vyzvedl; `JR_ko` vytvořil 15 za 50, dosáhl přesně 4 dnešních výher a odměnu vyzvedl; `t3X5` vytvořil 10 za 50 a má přesně 4 výhry, zatím nevyzvednuto. Jde o povolenou mezeru v pravidlech/ekonomický boost, ne o ovlivnění RNG; dnešní audit neprokázal významný funnel mezi účty se sdílenou IP/zařízením. Bez změny kódu, deploye nebo produkčních dat.
- **[Codex] Coinflip audit hráčů a strategie 50 → vyšší sázka — produkce read-only** — V `/data/app.db` a aktivním release ověřeno 25 493 dokončených coinflipů: celkově vyhrává p1 v 50,08 %, dnešní snapshot 667 her měl 50,07 %; 0 nesouladů výsledek/mince/výherce, 0 dvojitých výplat. Zakladatelé, kteří po už známém výsledku sázky 50 do 15 minut navýšili, dlouhodobě vyhráli 49,52 % z 1 351 případů; při navýšení na 500+ 52,92 % z 274, bez statisticky významné výhody. Dnešní nejaktivnější `monstrum2` byl 153/282 (+34 109), ale historicky 1 220/2 410 (50,6 %, běžná variance); `JR_ko` naopak dnes 2/12. Sdílená IP/zařízení neukázala významný jednostranný tok mezi aktivními dvojicemi. Produkční `coinflip_rig_*` settingy nejsou nastavené, tedy rig je dnes vypnutý; nasazený kód ale stále obsahuje skrytý konfigurovatelný rig hook, takže hra není z principu provably fair. Bez změny kódu, deploye nebo produkčních dat.
- **[Codex] Nápověda Statku: max. level 25 — NASAZENO (cache 2026071268)** — Z izolovaného produkčního release nasazena jediná textová oprava `max. level 10` → `max. level 25`; herní logika ani data beze změny. Zachován nový `SOON` Trh i předchozí UI úklid. Predeploy 498/498, frontend parser a `node --check` prošly; atomický deploy/restart na Contabo uspěl, health `app=ok`, `db=ok`, `maintenance=off`. Veřejný app.js potvrzuje nový text a nepřítomnost starého, index načítá cache 1268; Cloudflare Purge Everything dokončen ručně.
- **[Codex] Nápověda Statku: max. level 25 — lokálně, NENASAZENO** — Zastaralý text `max. level 10` opraven na `max. level 25`; herní logika už správně používá `MAX_LEVEL = 25`, takže bez změny pravidel či dat. Ověřeno `node --check`, `git diff --check` a kontrolou konstanty v `app/farm.py`.
- **[Codex] Komunitní Trh SOON redesign podle vybraného mocku — NASAZENO (cache 2026071267)** — Po uživatelově kontrole testovacího náhledu a výslovném pokynu nasazen z izolovaného produkčního release nový tmavě-zlatý teaser: velký titulek/režimy vlevo, dvě neklikací preview karty skinů vpravo, stav `Spouštíme brzy` a jemné farmářské pozadí. Produkce dál obsahuje jen `SOON` frontend bez marketplace formuláře/backendu; Early přepínač a farm gate banner zůstávají odstraněné. Žádná DB migrace ani změna produkčních dat. Predeploy 498/498 + frontend parser/`node --check` prošly, atomický release/restart na Contabo uspěl, health `app=ok`, `db=ok`, `maintenance=off`. Veřejný index/app.js/styles ověřeny s cache 1267, všechny 4 použité assety vrací 200 a Cloudflare Purge Everything proběhl ručně přes existující token.
- **[Codex] Komunitní Trh SOON redesign podle vybraného mocku — lokálně, TEST SERVER 127.0.0.1:8019, NENASAZENO (cache 2026071272)** — Původní prázdný teaser nahrazen tmavě-zlatým dvousloupcovým hero panelem: velký titulek a režimy vlevo, dvě překryté neklikací preview karty skinů vpravo, stav `Spouštíme brzy` a jemné farmářské pozadí. Reuse existujících assetů `statek-hero-c`, `klas-ready`, `stiletto-scorched` a `awp-printstream`; bez backendu a bez změny dat. Přidán pouze lokální QA query `?market-soon=1`, produkční hostname dál ukáže teaser automaticky. `node --check`, HTTP/cache 1272 a `git diff --check` prošly. Design QA v `design-qa.md` je `blocked`: in-app browser i Chrome opakovaně timeoutly při lokálním screenshotu, takže desktop/mobile vizuální shoda není vydávána za ověřenou.
- **[Codex] Odstranění Early/gate UI — NASAZENO (cache 2026071266)** — Z čistého izolovaného release zachovávajícího produkční `SOON` Trh nasazen frontend bez přepínače `🎫 Early` v seznamu uživatelů a bez banneru/akcí `Statek: VEŘEJNÝ / Vrátit za gate`; maintenance banner zůstal. Žádná DB migrace ani změna produkčních dat. Predeploy 498/498, frontend parser a `node --check` prošly; atomický deploy/restart na Contabo uspěl, veřejný index načítá cache 1266, veřejný app.js potvrzuje oba prvky odstraněné a `SOON` Trh zachovaný. Health `app=ok`, `db=ok`, `maintenance=off`; Cloudflare Purge Everything proveden ručně přes existující token, protože izolovaný worktree token záměrně neobsahuje.
- **[Codex] Odstraněné zastaralé Early/gate UI — lokálně, TEST SERVER 127.0.0.1:8019, NENASAZENO (cache 2026071271)** — Z admin seznamu uživatelů odstraněn přepínač `🎫 Early` a z horní části admin panelu celý banner `Statek: VEŘEJNÝ / Vrátit za gate` včetně mrtvých frontendových akcí; běžný maintenance banner zůstal. Backendová data ani veřejný Statek/Zahrádka se nemění. Ověřeno `node --check`, `git diff --check` a vykresleným admin tokem: seznam uživatelů funguje, 0 Early ovladačů, 0 farm gate bannerů/akcí, bez console warn/error. Screenshot vestavěného prohlížeče timeoutnul po úspěšné DOM/interakční kontrole.
- **[Codex] Komunitní Trh: produkční pojistka pro budoucí deploy — lokálně, NENASAZENO** — Rozpracovaná plná verze Trhu nyní mimo localhost vždy zobrazí pouze panel `SOON`; lokální test server zůstává plně funkční. Produkční endpoint pro odeslání nabídky vrací 503 „Komunitní Trh spustíme brzy.“, takže nelze omylem přijímat nabídky před spuštěním. Ověřeno `py_compile app/routers/auctions.py`, `node --check web/app.js`, 16/16 cílených testů a `git diff --check`. Aktuální produkce už obsahuje samostatný SOON teaser.
- **[Codex] Komunitní Trh jako produkční „SOON“ teaser — NASAZENO (cache 2026071265)** — Z čistého izolovaného worktree na `53ae0b0` nasazen pouze frontendový teaser bez rozpracovaného marketplace backendu a bez ostatních lokálních změn: Shop má novou záložku `🌾 Trh`; po kliknutí skryje běžné filtry/aukční blok a zobrazí panel `SOON · Komunitní Trh` s informací o připravované pevné ceně nebo aukci. Produkce zatím nepřijímá nabídky (`POST /api/auctions/submissions` = 405), nevznikla žádná DB migrace ani změna produkčních dat. Predeploy 498/498 + frontend parser/`node --check` prošly, atomický release na Contabo a restart byly úspěšné, health app/DB 200 (`maintenance=off`), veřejný index/app.js ověřen s cache 1265 a Cloudflare Purge Everything dokončen ručně přes existující token po tom, co izolovaný worktree token záměrně neobsahoval.
- **[Codex] Trh: pevná cena nebo aukce + zrušení veřejného uploadu — lokálně, TEST SERVER 127.0.0.1:8019, NENASAZENO (cache 2026071270)** — Divák ve formuláři volí `Pevná cena` nebo `Aukce na 24 hodin`; admin ve frontě typ vidí a schválením jej zachová. Pevná nabídka má na Trhu cenu a jediné tlačítko Koupit, nemá časovač/příhozy a server příhoz odmítá. Aukce má vyvolávací cenu, příhozy a bez `Kup teď`; pevné prodeje se nepletou do aukčního banneru ani žebříčku dražitelů. Veřejné `Z PC`, file input i `/api/auctions/upload-image` byly odstraněny; zůstává URL a katalog skinů. Ověřeno na test serveru oběma typy, blokací příhozu do pevné nabídky (400), odstraněným uploadem (405), čekající ukázkou v adminu a plnou pytest suite; Python compile, `node --check` a `git diff --check` prošly. Vizuální QA znovu `blocked`, protože in-app browser po načtení Shopu zamrzl na interakci i screenshotu; funkční E2E tok je ověřený. Produkce ani produkční data beze změny.
- **[Codex] Divácké nabídky skinů se schválením — lokálně, TEST SERVER 127.0.0.1:8019, NENASAZENO (cache 2026071269)** — V záložce `🌾 Trh` otevře CTA přihlášenému divákovi formulář ve stylu Shopu: název, obrázek přes URL / upload z PC / katalog skinů, popis, Steam inspect link a požadovaná cena. Odeslaná nabídka zůstane neveřejná; v `Admin → Aukce` se objeví fronta s obrázkem, prodejcem, cenou, inspect/trade odkazy a akcemi schválit nebo zamítnout. Schválení atomicky vytvoří 24h komunitní aukci s `Kup teď`, zamítnutí i schválení pošle divákovi notifikaci; bezpečnostně je omezen Steam link, velikost/typ uploadu, rate limit a max. 5 čekajících nabídek. Na odděleném test serveru ověřeno odeslání, admin fronta a schválení: vznikla aukce `M4A1-S | Printstream (Minimal Wear)` za 22 000, jedna ukázková `AK-47 | Redline (Field-Tested)` za 18 000 zůstává čekat pro ruční kontrolu. Plná pytest suite, Python compile, `node --check` a `git diff --check` prošly. Vizuální browser QA je v `design-qa.md` označeno `blocked`, protože oba dostupné browserové náhledy opakovaně zamrzly při lokální interakci; API/E2E logika je ověřena. Produkce ani produkční data beze změny.
- **[Codex] Komunitní Trh přesunutý do filtrů Shopu — lokálně, TEST SERVER 127.0.0.1:8019, NENASAZENO (cache 2026071268)** — Opraveno původní nepochopení návrhu: komunitní nabídky už nejsou velký blok nad Shopem. `🌾 Trh` je pátá záložka vedle Vše / Instantní / Tombola / Končí brzy a teprve po jejím zvolení nahradí produktový grid skiny diváků ve stejném kartovém stylu. Trh má stručný ověřovací/payout kontext a jedinou CTA „Prodat vlastní skin“; běžný Shop komunitní nabídky nezobrazuje a oficiální aukce zůstávají oddělené. Ověřeno `node --check`, `git diff --check`, 21/21 cílených auction/ticket testů a automatickým UI průchodem na desktopu 1440×1000 i mobilu 390×844: přepnutí Trh → Vše, ukázková AWP nabídka/prodávající, prodejní connect flow, bez horizontálního overflow, console errors a page errors. Produkce ani produkční data beze změny.
- **[Codex] Komunitní komisní trh skinů — lokálně, TEST SERVER 127.0.0.1:8019, NENASAZENO** — Nad stávajícími aukcemi doplněn volitelný prodávající: komunitní skin je veřejně označený jménem prodejce, prodejce nesmí vlastní nabídku dražit ani vykoupit a po časovém konci nebo „Kup teď“ dostane atomicky právě jednou 95 % ceny; 5 % se ukládá jako market fee/sink. Běžné aukce bez prodávajícího zůstávají plným sinkem. Admin při vystavení zadá nick prodávajícího (server ověří existující nezablokovaný účet), vidí výplatu/poplatek a UI připomíná vystavit až po převzetí skinu. Shop má samostatnou sekci „Komunitní trh“, ověřenou kartu a „Prodat skin“, které přihlášenému otevře nový support ticket `market/sell` s přesnými instrukcemi a Steam bezpečnostním varováním. Přidány idempotentní DB sloupce/migrace a cílené testy pro buy-now, časovou finalizaci, self-buy blok i ticket kategorii. Plná suite prošla 500/500, Python compile, `node --check`, `git diff --check`, health a veřejné API OK. Browser QA: Shop, prodejní modal a admin pole prodávajícího vykresleny bez console warn/error; mobilní viewport override testovacího browseru se neaplikoval, proto mobilní breakpoint není označen za vizuálně ověřený. Oddělená test DB `.codex-test-market-data` obsahuje ukázkovou nabídku AWP Printstream od BěžnýDivák; produkce ani produkční data beze změny.
- **[Codex] Návrh komunitního bazaru skinů — lokálně, rozhodnutí** — Po kontrole existujících aukcí, Steam trade linků a podpory doporučen nejmenší bezpečný pilot jako komisní trh: prodávající přihlásí skin přes support ticket a předem ho pošle adminovi, admin jej vystaví ve stávající aukci s „Kup teď“ a po dokončení dostane prodávající výnos v sedlácích minus 5% sink poplatek. Tím odpadá nový P2P spor „odeslal/neodeslal“, protože skutečný skin i sedláky jsou před vypořádáním pod kontrolou webu/admina. Samostatné tržiště, automatické ověřování inventáře a trade bot až podle reálného objemu. Kvůli Steamem výslovně nepodporované směně itemu za hodnotu mimo jedno trade okno musí UI jasně varovat před rizikem a pilot zůstat moderovaný. Bez změny aplikačního kódu, produkce či dat.

## 2026-07-18

- **[Codex] Ekonomický rozbor Statek + Zahrádka — lokálně, rozhodnutí** — Z aktuálních konstant a toku odměn ověřeno: Zlatý klas má nejvyšší přímý výnos i XP na záhon/hodinu, ale každá sklizeň bez ohledu na plodinu dává právě 1 krmivo. Doporučená kombinovaná strategie proto je klas jako výchozí plodina, při nedostatku krmiva krátce rychlé plodiny (prakticky brambory; dýně pro méně časté přihlášení), po doplnění zásoby zpět na klas. Na Statku má na lvl 1 nejvyšší průběžný čistý výnos na slot Kůň pro SUB (87,5/h při placeném, 137,5/h s krmivem), jinak Kráva (75/h, resp. 116,7/h); startovní Slepice za 500 má nejrychlejší návratnost. Bez změny herních pravidel, kódu, produkce či dat.
- **[Codex] Hráč 516 `op_domy`: kompenzace chybějícího 1× gifted subu — prod-data** — Po výslovném schválení provozovatele atomicky dorovnán screenshotem potvrzený gift pro `seb1n_1`: gifterovi +2 360 bodů (`Kick gift sub 🎁 ×1 (happy 2×)`; crew lvl 10 bonus +18 %), +5 000 supporter XP/crew contribution, +1 wheel spin a +1 nevyužitý farm turbo token; zároveň +3 000 bodů za historicky náležející sub-goal tier 3. Aktuální sub cíl se kvůli mezitím přijatému legitimnímu resubu posunul 37→38, řádek giftera má `subs=1`, `hh_subs=1`, `paid_tier=3`. Příjemce 1996 `seb1n_1` dostal recipient log a aktivní sub/roli `sub`; protože chybějící Kick payload neobsahoval dostupné `expires_at`, použit standardní fallback +32 dní (do 2026-08-19 21:24:37 UTC). Jednorázový `claim_locks` klíč brání opakování ruční kompenzace. Záloha před zásahem: `/data/backups/pre-compensate-gift-516-20260718T212127Z.db` (`quick_check=ok`); po zásahu produkční `quick_check=ok`, health app/DB OK, maintenance off. První pokus před změnou bezpečně selhal kvůli chybějícímu `WEBOS_DATA_DIR`; vzniklý prázdný 0B soubor v release byl ověřen a odstraněn, produkční DB se ho nedotkla.
- **[Codex] Hráč 516 `op_domy`: Kick Activity Feed potvrzuje chybějící 1× gift — produkce read-only** — Screenshot Kick Activity Feed potvrzuje `op_domy just gifted 1 sub to: seb1n_1` přibližně v 20:55 UTC, mezi úspěšně přijatými eventy `jeanette_9947` (20:54:39) a `MarekMH` (20:55:10); aplikace tento jediný event nemá. V době giftu běžela happy hour a hráčova crew Vopice je lvl 10 (+18 %), takže plná historicky věrná kompenzace by byla: +2 360 gift bodů, +5 000 supporter XP/crew contribution, +1 wheel spin, +1 farm turbo token, příjemce `seb1n_1` jako sub, sub-goal progress 36→37 a zápis giftera 1/1 HH. Protože přišel před dosažením tieru 3, náleží mu navíc +3 000 odměna sub cíle; celkový zůstatek tedy +5 360. Bez zásahu do produkčních dat, čeká na výslovné schválení kompenzace.
- **[Codex] Hráč 516 `op_domy`: kontrola gifted subs — produkce read-only** — V `/data/app.db` nemá v aktuální sub-goal relaci žádný záznam v `subgoal_gifters` ani dnešní `Kick gift sub` pohyb bodů; poslední evidovaný gift je 1× z 2026-07-13. Dnešní cíl má progress 36 (33 giftů + 3 sub/resub) a všech 20 doručených gift webhooků od startu relace je konzistentně zapsaných jiným gifterům; journal neobsahuje `op_domy`, anonymní/bez-giftees gift ani chybu/odmítnutí webhooku. Závěr: tvrzený gift event od Kicku do aplikace nedorazil, proto body ani cíl nebyly změněny. Bez zásahu do produkčních dat.
- **[Codex] Crew War `Interatyho Klubík` vs `MODI KOKOTI [MODS]` — prod-data** — Na výslovný pokyn provozovatele jednorázově obejit zbývající cooldown cílové crew (80 096 s, ~22 h 15 min) bez trvalé změny kódu/pravidel. Stávající `crews.declare_war` vytvořil aktivní válku ID 56 (crew 1 → 25), start 2026-07-18 21:01:14 UTC, konec 2026-07-19 21:01:14 UTC, start XP 1 948 314 vs 123 452; oba `war_start` logy a členské notifikace vznikly. Konzistentní SQLite záloha před zásahem: `/data/backups/pre-force-crew-war-1-vs-25-20260718T210112Z.db` (`PRAGMA quick_check=ok`). První bezpečnostně zastavený pokus nevytvořil válku ani nezměnil DB kromě extra zálohy.
- **[Codex] Crew War: viditelný cooldown soupeřů — NASAZENO (cache 2026071265)** — Nasazen izolovaný atomický release z aktuálního produkčního základu, aby neodvezl rozpracované zrušení ochrany darů ze stejné IP/zařízení. Okno vyhlášení války ukazuje u každé party „ve válce“, přesný zbývající cooldown, nebo aktivní tlačítko; vlastní crew vidí svůj cooldown také. Predeploy 498/498, frontend parser OK, restart/health/DB OK a Cloudflare Purge Everything dokončen.
- **[Codex] Crew War: viditelný cooldown soupeřů — lokálně, NENASAZENO** — Backend crew žebříčku a detailu nově vrací přesný zbývající cooldown války v sekundách. Okno „Vyhlásit válku“ už neschovává nedostupné party: u každé ukáže aktivní válku, zbývající cooldown (např. 47h 12m), nebo aktivní tlačítko pro vyhlášení; vlastní crew během cooldownu vidí stejný čas a vypnuté tlačítko. Pravidla války beze změny. Ověřeno 55/55 testy `tests/test_crews.py`, `node --check web/app.js` a `git diff --check`.
- **[Codex] Produkční smoke/debug audit `zurys.live` — lokálně, produkce read-only** — Prošlo 28 známých SPA tras na desktopu, 8 hlavních tras na mobilu 390×844 a všech 19 dostupných admin karet; kontrola identity stránky, smysluplného renderu, framework overlayů, konzole, rozbitých obrázků, skeletonů a horizontálního overflow. Root, `/api/health` a `/api/monitor/healthz` vrací 200 (app/DB OK, maintenance off); shop filtr Tombola a mobilní menu fungují. Potvrzen 1 frontend bug: rychlé přepnutí admin karet `Bezpečnost` → `CaseHug` nechá aktivní `CaseHug`, ale pozdní async odpověď vykreslí obsah `Bezpečnost` — `renderAdminTab()`/async `admin*()` zapisují po `await` do společného `#adminContent` bez kontroly aktuální karty. Bez opravy, deploye a změny produkčních dat; cizí rozpracované změny v ticket podpoře zachovány. In-app browser při DOM/screenshot kontrole zamrzl, audit dokončen přes dostupný Chrome; důkazní screenshoty jsou mimo repo.
- **[Claude] Aukce: hero obrázek vrácen na banner nahoře — NASAZENO (cache 2026071257)** — user chtěl velké obrázky zpět („ty obrázky vrat zpátky prosim"); boční 126px thumb z 1255 skiny moc zmenšoval. CSS revert na column layout + `aspect-ratio: 2.2/1` banner; ZŮSTÁVÁ z 1255: timer v řádku s názvem (`.auc-top`, ne overlay), „🔨 N×", fee popover, sbalitelný žebříček, fix svislé čáry. Jen `web/styles.css`. Ověřeno na dev (column, hero přes šířku karty), deploy predeploy 496/496 + health 200 + CF purge, živě grep `aspect-ratio: 2.2 / 1` v prod CSS. Pozn.: Codexův security audit níž zmiňuje „cizí rozpracované změny" ve worktree — to byla tahle změna, už je nasazená a commitnutá.
- **[Codex] Repository-wide Codex Security audit revize `d3331fb` — lokálně** — Standard scan uzavřel 86/86 full-file review receipts a 25/25 coverage surfaces; z 27 raw kandidátů vzniklo po deduplikaci 26 validačních řádků a po threat-model/policy kalibraci 16 finálních nálezů (5 high, 8 medium, 3 low). Cílené existující testy prošly; bez produkčních requestů, bez čtení nebo změny produkční DB a bez deploye. Kanonické JSON artefakty byly lokálně zapečetěny a `report.md` vygenerován; workbench nemohl původní scan označit jako dokončený, protože `main` během auditu postoupil z `d3331fb` na `e80d1f3` a worktree obsahuje cizí rozpracované změny, které Codex zachoval. Podrobné samostatné write-upy a hardening portfolio přeskočeny na výslovné přání uživatele.
- **[Claude] FIX svislá čára u rozbalených `<details>` + kompaktní horizontální aukční karty — NASAZENO (cache 2026071255)** — (1) **Čára**: `.section-title::after` je dekorativní linka (`flex:1` + gradient); pravidlo šipky `.lb-fold > summary.section-title::after` přepisovalo jen `content` na „▸", roztažení a gradient zůstaly → šipka roztažená přes šířku panelu, po `rotate(90deg)` při rozbalení svislá čára přes stránku. Fix `flex:none; height:auto; background:none` v pravidle šipky — spraví VŠECHNY `.lb-fold` v appce (Top zahradníci, sezónní žebříčky…), bug tam byl latentně od zavedení foldů. (2) **Kompaktní karty**: hero obrázek z banneru nahoře (2.2:1) na levý panel 126px (mobil ≤480px: 92px), timer z overlay do řádku s názvem (`.auc-top`), „N příhozů"→„🔨 N×" (plné znění v title). Naměřeno: desktop karta 357→**193px**, mobil 375px ~350→**203px**, bez overflow. Verifikace na dev serveru vyžadovala lokální cache bump (SW drží shell dle `?v=`, hash-navigace nereloadne dokument — nový tab + bump na 2026071254); deploy pak bumpnul na 1255. Predeploy 496/496, health 200, CF purge, živě ověřeno grep nového markup v prod app.js/styles.css.
- **[Claude] Aukce: mobile fee tooltip + social proof + sbalitelný žebříček — NASAZENO (cache 2026071253)** — (1) fee ikonka 🎟 byla hover-only (`title` atribut) → na mobilu (většina diváků) nešla vůbec zobrazit; teď vlastní popover (`.auc-fee-pop`) co jde otevřít i klikem/tapem (`data-action="auction-feetip"` toggluje `.open`), hover pro desktop zůstal. (2) u ceny se nově ukazuje `🔨 N přihozů` (`bids_count` už bylo v API, jen se nevykreslovalo) — sociální důkaz bez odhalení identity dražitelů. (3) „Top dražitelé" sbalen do `<details class="lb-fold">` (stejný nativní vzor jako u ostatních žebříčků v appce) — zavřený defaultně, klik rozbalí; reakce na „je to naplácané na jedné stránce". Čistě `web/app.js` + `web/styles.css`, žádná změna backendu/testů (14/14 auction testů beze změny prošlo). Ověřeno: `node --check`, přímý curl serveru (browser preview měl cache glitch, obešel jsem ho). `python deploy.py --deploy`: predeploy 496/496 + frontend OK, atomický release, health 200 + CF purge. Živě ověřeno na zurys.live (curl grep nový markup).
- **[Claude] Aukce: menší/přehlednější karta — NASAZENO (cache 2026071252)** — reakce na dojem „karty jsou moc velké". Hero obrázek 16:9→2.2:1 (−43px na 400px kartě), smazán redundantní štítek „AUKCE O SKIN" (sekce nahoře už má „LIVE aukce") i duplicitní 🔴 LIVE badge na kartě, dlouhá fee poznámka nahrazena ikonkou 🎟 s nativním tooltipem vedle ceny, quick-bid chipy 3→2, název aukce zkrácen na 1 řádek (ellipsis). Čistě `web/app.js` (auctionCardHTML) + `web/styles.css`, žádná změna logiky/testů. Ověřeno na dev serveru přes DOM measurements (screenshot tool padal, known issue): karta 450px→357px výšky, tooltip text OK, 2 chipy. `python deploy.py --deploy`: predeploy 496/496 + frontend OK, atomický release, health 200 + CF purge. Živě ověřeno: prod app.js bez „AUKCE O SKIN", s `auc-fee-tip`, bez 3. chipu.
- **[Claude] Aukce: vstupní poplatek — NASAZENO (cache 2026071251)** — `python deploy.py --deploy`: predeploy 496/496 + frontend OK, maintenance vypnutá, atomický release, health 200 + DB ok, CF Purge Everything. Živě ověřeno: prod `auctions.py` má `ENTRY_FEE_CAP`, `OUTBID_REFUND_PCT` 0 výskytů, prod DB má sloupec `auction_bids.fee` (migrace proběhla restartem), veřejný app.js v=2026071251 obsahuje 🎟 vysvětlivku i fee toast. Detaily featury v záznamu níž.
- **[Claude] Aukce: 10% srážka při přehození → jednorázový vstupní poplatek — lokálně (nasazeno viz výš)** — reakce na stížnosti („bere mi to 10 % z částky"). `OUTBID_REFUND_PCT 0.9` smazán; přehozenému se vrací **100 %** escrow. Sink nově = vstupní poplatek při PRVNÍM příhozu do dané aukce: `ENTRY_FEE_PCT 10 %` z prvního příhozu se stropem `ENTRY_FEE_CAP 5000` (velké aukce flat 5k dle přání ownera, malé úměrně míň — flat 5–10k by malé aukce zabil). Další příhozy téhož uživatele bez poplatku → bid war nekrvácí. Poplatek si nechává i vítěz/vykoupený; zrušení aukce vrací escrow i poplatky (nový sloupec `auction_bids.fee` — migrace + SCHEMA; staré aukce fee=0 → cancel no-op). Opraven i stale text notifikace („50 %" z doby půlkové srážky). Frontend: vysvětlivka 🎟 pod bid řádkem, toast ukazuje zaplacený poplatek, stale komentáře pryč. Testy: všech 8 dotčených přepočítáno + nový `test_entry_fee_once_capped_and_refunded_on_cancel` (strop, jen-jednou, vracení); plná suite 496/496, `node --check` OK. Živě na dev serveru (dev DB, injektovaná session): bid 60k → strženo 65k, `fee:5000` v odpovědi, cancel vrátil přesně vše; dev data uklizena. Deploy až na výslovné „nasaď".
- **[Claude] Denní strop Statku navázán na stodolu — NASAZENO (cache 2026071250)** — reakce na stížnost Martech1 (750k utopených ve statku, strop 20k trefuje na stodole lvl 2). Místo plošného zvednutí (inflace) strop nově roste se stodolou: `FARM_DAILY_FULL 20000 + FARM_DAILY_PER_BARN 5000 × (barn_level − 1)` → lvl 5 = 40k/den plné sazby; nad strop dál ×0,25 (`FARM_SOFT_RATE` beze změny). Tím prestige stodola (sink 50k–1M) kupuje i denní kapacitu, ne jen slot — do teď sloty navíc jen rychleji narážely do stropu a marginální hodnota sinku klesala. Nový helper `farm._daily_full(conn, uid)` (jediný zdroj pravdy) použitý v `_award_product` i `status()` → frontend chip 📈 dnes N/M se přizpůsobí sám. UI texty: tooltip chipu + tlačítko stodoly „(+1 slot · +5k denní strop)". Nový test `test_barn_raises_daily_cap` (lvl 3: plný výnos na 20k, soft nad 30k; status kontrakt). Ověřeno lokálně živě (dev DB, curl s injektovanou session: `farm_daily_full: 30000` při barn 3; session uklizena). Deploy `python deploy.py --deploy`: predeploy 495/495 + frontend OK, maintenance ověřena vypnutá, atomický release, health 200 + DB ok, CF Purge Everything. Živě ověřeno: prod `farm.py` obsahuje `_daily_full`/konstantu, veřejný app.js v=2026071250 má oba nové texty. Bez změny produkčních dat. Pozn.: deploy odvezl i Codexův UX balíček 2026071249 (v tree byl nasazený, ale necommitnutý) — no-op, commit dorovnává repo na produkci.

## 2026-07-17

- **[Codex] UX balíček 2/3/4/6/7/8 — NASAZENO (cache 2026071249, release webos-1784305600079870900)** — `python deploy.py --deploy`: predeploy 494/494, frontend balance a `node --check` OK; atomický release na Contabo, restart/health/DB OK, údržba vypnutá a Cloudflare Purge Everything dokončen. Živě ověřeno: veřejný build 2026071249 obsahuje rychlé akce, týdenní souhrn i stav darů; produkční `/data/app.db` má novou tabulku `support_ticket_events`. Bez ruční změny produkčních dat.
- **[Codex] UX balíček 2/3/4/6/7/8 — lokálně, NENASAZENO** — Homepage má kontextové rychlé akce (sklidit/sebrat/nakrmit vše, bonusy, úkoly), rozšířený onboarding (první zvíře + vstup do party) a osobní týdenní souhrn. Směnárna nově ukazuje posledních 30 odeslaných/přijatých darů včetně stavu schválení. Podpora dostala auditní timeline ticketu a samostatný atomický refund endpoint, který zapisuje body, ticket event i globální admin audit v jedné transakci. Statek jasně vysvětluje max. level 10 a výjimku psa (nekrmí se/neleveluje, +10 % a ochrana před liškou). Přidána nová tabulka `support_ticket_events`, API testy a responzivní styly. Ověřeno cíleně 14/14 a celým pytest balíkem, `compileall`, `node --check` a `git diff --check`; in-app browser po úvodním načtení opakovaně zamrzl při DOM/screenshot čtení i v čisté kartě, proto vizuální QA zůstává neověřené. Bez deploye a bez změny produkčních dat.
- **[Codex] Zdravotní audit projektu — lokálně, produkce read-only** — čistý `main` shodný s `origin/main`; predeploy 491/491, frontend balance, `node --check`, `compileall`, `pip check`, `git diff --check` a Git connectivity OK. Produkce `zurys.live`: public + deep health 200 (`app/db ok`, maintenance off, alerts configured), chráněný ticket endpoint 401 bez session, veřejné `app.js`/`styles.css` SHA-256 shodné s lokálem, cache 2026071248. Citlivé lokální soubory jsou správně gitignored a ve verzovaných souborech nebyl nalezen klíč. Nalezený dluh: `DEPLOY.md`, část README, komentáře a 4 skripty stále odkazují na zrušené Fly.io; test suite používá session-scoped sdílenou DB a při plném běhu vypisuje 2 očekávané webpush chyby. Bez změny kódu, deploye nebo prod dat.

## 2026-07-16

- **[Claude] Podpora batch 1+2+3 — NASAZENO (cache 2026071248, commit ac7500a)** — `python deploy.py --deploy`: maintenance_mode ověřen '0' před deployem, predeploy 491/491 + frontend OK, atomický release, health 200 + DB ok, CF Purge Everything. Živě ověřeno: prod app.js (v=2026071248) obsahuje nový support kód, `POST /tickets/{id}/attach` bez session 401, migrace `support_ticket_messages.image` proběhla. Commit + push na origin/main (main = stav produkce). Detaily featur v záznamech níž.
- **[Claude] Podpora: admin vylepšení (Discord alert + kontext uživatele + šablony) — lokálně, NENASAZENO** — (1) **Discord alert**: `app/routers/tickets.py` volá `alerts.send()` při novém ticketu (`🎫 Nový ticket #N od X: předmět` + kategorie/preview/odkaz, key per ticket) a při odpovědi uživatele (`💬 Ticket #N – nová zpráva`, cooldown 300 s per ticket) — bez `DISCORD_ALERT_WEBHOOK` no-op, admin reply alert nespouští. (2) **Kontext uživatele**: `GET /tickets/admin/{id}` nově vrací `user_ctx` (ID, sedláci, level z earned_total, ban, člen od, počet ticketů, poslední 3 objednávky s product_name) a frontend ho renderuje jako kartu pod meta řádkem detailu s proklikem na `#/u/<nick>` — admin u refund/sub stížností nemusí do admin panelu. (3) **Šablony odpovědí**: 3 tlačítka 📋 nad composerem (sub import / refund hotovo / chci víc info), čistě frontend (`SUPPORT_TEMPLATES` v app.js), klik předvyplní textarea. Soubory: `app/routers/tickets.py`, `web/app.js`, `web/styles.css`. Ověřeno: pytest test_tickets OK, browser QA na dev DB (ctx karta renderuje, šablona vyplní composer), obě alert větve přes API 200 (user reply + create). Deploy až na výslovné „nasaď".
- **[Claude] Podpora batch 3: in-app notifikace + FAQ + přílohy + digest + points-feed proklik — lokálně, NENASAZENO** — (7) **Zvoneček**: `_add_message()` volá `notify()` když píše staff (🎫 „Podpora odpověděla na ticket #N", link `#/podpora/N`); ruční resolved/closed od admina notifikuje taky. (8) **Mini-FAQ**: `SUPPORT_CATEGORIES[*].faq` (frontend const) — 1–2 nejčastější odpovědi se ukážou v modalu nového ticketu nad formulářem (💡 box), může srazit počet ticketů. (9) **Přílohy**: `POST /tickets/{id}/attach` (vlastník/admin, rate limit 5/5min) — data URL, validace SDÍLENÁ s uploady odměn (`_decode_image_dataurl`: PNG/JPG/WEBP/GIF, 6 MB, přípona dle MIME), soubor `/uploads/ticket_<hex>.<ext>`, zpráva s novým sloupcem `support_ticket_messages.image` (migrace v db.py); frontend 📎 u composeru + náhled obrázku v bublině (`dmBubbles` renderuje `m.image`, DM zprávy pole nemají → beze změny). (10) **Digest**: řádek `🎫 Otevřené tickety: N (nejstarší bez reakce X h)` v denním Discord digestu. (11) **📜 Pohyby bodů**: tlačítko v admin kontextové kartě → `#/admin` security→points s předvyplněným ID účtu (`adminState.pfQuery`). Ověřeno: pytest tickets 3/3 (nový test: attach auth/404, bad formát 400, image v threadu, notifikace po admin reply i resolved) + dm/casehug 8/8, node --check, browser QA: FAQ box renderuje, 📎 upload z UI end-to-end (bublina s `/uploads/ticket_*.png`), 📜 otevře points feed s query 235 a 1 řádkem, konzole čistá, digest řádek živě ověřen. Deploy až na výslovné „nasaď".
- **[Claude] Podpora batch 2: user resolve + refund z ticketu + auto-close — lokálně, NENASAZENO** — (4) **„Vyřešeno, díky"**: nový `POST /tickets/{id}/resolve` (jen vlastník, 409 když už resolved/closed) + tlačítko ✅ v detailu pro ne-staff; admin flow beze změny. (5) **Refund z ticketu**: v admin kontextové kartě pole + tlačítko „💰 Připsat body (refund)" — volá EXISTUJÍCÍ `POST /admin/users/{id}/points` s reason `Refund: ticket #N` (= classify_xp zero, žádné XP/žebříčky, audit log jede), žádný nový backend. (6) **Auto-close**: resolved ticket bez aktivity 7 dní (`AUTOCLOSE_DAYS`) se zavře líným sweepem `_autoclose()` při načtení `/tickets/mine` a `/tickets/admin/all` — žádný daemon, žádný write na polling hot-path (`/tickets/unread` sweep nevolá). Test `test_resolve_own_user_ctx_and_autoclose` (user_ctx jen pro admina, resolve auth/409, autoclose po 8 dnech). Ověřeno: pytest tickets 2/2, node --check, browser QA (refund přes API zapsal points_log `Refund: ticket #6` bez růstu earned_total; klik uživatele na ✅ přepnul ticket na resolved; user nevidí ctx/refund/šablony; konzole bez chyb). Deploy až na výslovné „nasaď".

- **[Claude] Perf batch: microcache + indexy — NASAZENO (cache 2026071247)** — okno off-stream, prevence dalšího „battlepass momentu". (1) **`app/microcache.py`** — `@ttl_cache(s)` dekorátor pro globální read-only endpointy (klíč ignoruje conn/user Row → auth běží dál per request, cachuje se jen výpočet; klíčuje dle ostatních args, pojistka 64 klíčů). Nasazeno na: stream/status+nx/state (3 s), auctions (2 s), sub-goal+happy-hour+shop/activity+shop/recent (5 s), leaderboard (10 s), crews/tags (30 s) → při live zmizí ~80 % SQL hot-path práce, staleness ≤ TTL. (2) **Indexy** (SCHEMA, IF NOT EXISTS, staví se při bootu): `idx_points_log_user_reason(user_id, reason, change)` covering pro questové SUMy — **7,8 ms → 0,16 ms (49×)** u nejtěžšího uživatele (11,4k řádků); + `idx_dropclaims_user`, `idx_pred_bets_user`, `idx_duels_p2` (OR větev pvp_won). Ověřeno: unit test cache (hit/klíč/expirace), DI podpis přes @wraps, EXPLAIN QUERY PLAN, predeploy 489/489, všech 8 endpointů živě 200 s validními daty. Commit navíc dorovnává repo na nasazený stav (crews/db/shop/main aj. nesly deployed-ale-necommitnuté změny z dnešních release).
- **[Claude] Valda_cz (1275): zrušeno omylem zapnuté trvalé sebevyloučení ze sázek — prod-data** — na žádost streamera (`gamble_block_until` 'permanent' → NULL, in-app notifikace 🎰 poslána). Revert: `UPDATE users SET gamble_block_until='permanent' WHERE id=1275`.
- **[Claude] Watchdog + limit-concurrency (pojistky po pádu) — NASAZENO na serveru (žádná změna kódu/release)** — (1) `/opt/webos/watchdog.sh` + `/etc/cron.d/webos-watchdog`: každou minutu curl `/api/health` (max 5 s; prochází údržbou i origin lockem) → 3× po sobě fail = `systemctl restart webos` + Discord alert (DISCORD_ALERT_WEBHOOK, testovací zpráva prošla 204); cooldown 10 min proti restart smyčce; stav v `/run/webos-watchdog/`; `DRYRUN=1` na testy; ověřeno zdravá cesta + 3× fail dry-run + reálný cron běh 20:49. (2) systemd override doplněn o `ExecStart … --limit-concurrency 256` (uvicorn při přetížení vrátí 503 místo kolapsu; zdravý provoz má jednotky–desítky souběžných spojení). Revert: smazat watchdog soubory; z override.conf odebrat ExecStart řádky + daemon-reload + restart. POZOR: přežije deploy (unit se nemění), ale NE rebuild serveru přes contabo_setup.sh — tam by se muselo doplnit.
- **[Claude] Výběr počtu tiketů v potvrzovacím modalu tomboly — NASAZENO (cache 2026071246)** — žádost: nekupovat tikety po jednom. `confirmBuyModal` má u raffle produktů stepper (−/+/ruční zápis), živý přepočet ceny + zůstatku + textu tlačítka (české skloňování tiket/tikety/tiketů); max = min(dovolí si, sklad, limit na osobu), server má finální slovo. Nákup N kusů = 1 request přes existující `/cart/checkout` (backend beze změny, atomicky N entries + orders, jede i s `_buyBusy` guardem). Ne-raffle beze změny. Ověřeno Playwrightem lokálně (service_workers="block"): qty 5 → přesně 1 POST /cart/checkout, DB 5+5, clamp 999→20 OK; testovací data uklizena. Deploy 489/489, health OK, CF purge OK.
- **[Claude] Shop double-submit guard + refundy duplicitních tiketů — NASAZENO (cache 2026071245) + prod-data** — GROOF890 nahlásil na DC „koupil jsem 35 tiketů místo 5, sekl se store". Ověřeno z prod dat: 7 identických checkoutů košíku (5× tiket 25€ GIFT CARD, produkt 125) během 6,3 s v 16:54 UTC — server byl v tu chvíli přetížený (viz pád webu níž), „mrtvé" kliky na Koupit prošly všechny. Druhý postižený týž den: SonnyVoodoo 2× identický 18kusový checkout (10€ GIFT CARD) 0,8 s po sobě ve 14:01. **Refundy (prod DB, points_log reason `Refund: …` = classify_xp zero, žádné XP/žebříčky):** GROOF890 smazáno 30 duplicitních raffle_entries p125 (5 tiketů zůstává) + orders 15938–15967, +15 000 sedláků (42 109→57 109); SonnyVoodoo smazáno 18 duplicitních orders 14869–14886 (10€ tombola už neexistuje, entries zmizely s ní), +1 800 (128 168→129 968). Oba dostali in-app notifikaci 🎫; ID smazaných řádků v session logu Claude. Ani jeden nic nevyhrál → losování nedotčena. **Fix příčiny:** `web/app.js` — `_buyBusy` guard v `buyProduct()` + `doCheckout()` (klik během běžícího nákupu se ignoruje, tlačítko disabled „⏳ Zpracovávám…", po chybě se uvolní). Ověřeno lokálně v prohlížeči: 3 rychlé kliky → přesně 1 POST; po 400 se guard uvolní a další klik projde. Deploy `python deploy.py --deploy` (489/489, cache 1245, health OK, CF purge OK) — tím se zároveň řádně nasadily oba hotfixy z pádu webu (battlepass + Sentry), prod už nemá hotfix divergenci vůči repu.
- **[Claude] PÁD WEBU vyřešen: battlepass baseline přepočet + Sentry integrace — HOTFIX NA PRODU (bez release), zrcadleno do repa** — web 2× spadl na `OSError: [Errno 24] Too many open files` (19:17 a ~19:37 CEST). Řetěz: `/api/me/claims` polling (~4/s při live) → `battlepass._row()` počítal `_season_baseline` = **Python průchod všech points_log řádků uživatele za sezónu PŘI KAŽDÉM volání** (15. den měsíce = tisíce řádků/chatter) → CPU meltdown → requesty se hromadí (každý drží DB conn + socket) → FD strop 1024 → `accept()` chrlí ~170 chyb/s → Sentry LoggingIntegration každou serializuje na event loopu (zesilovač) → smrt za ~20 min od restartu. Diagnóza py-spy (nainstalováno do venv; revert `pip uninstall py-spy`). **3 fixy:** (1) `app/battlepass.py::_row` — drahý dopočet jen při založení řádku sezóny, pojistka nahrazena levným `earned_total < baseline` (commit v repu); (2) `app/main.py` Sentry `disabled_integrations=[Starlette, FastApi, Logging]` — span/uuid4 mašinérie žrala ~60 % CPU event loopu i s `traces_sample_rate=0.0`; chyby dál chodí přes ruční `capture_exception` (hunk v repu, main.py má i cizí WIP → necommitováno); (3) systemd override `/etc/systemd/system/webos.service.d/override.conf` `LimitNOFILE=65536` (revert = smazat + daemon-reload + restart). Hotfixy aplikovány přímo na `/opt/webos/app` (zálohy `/root/main.py.bak-sentry-hotfix`, `/root/battlepass.py.bak-hotfix`) — **další deploy je přepíše, repo už fixy obsahuje** (main.py hunk čeká na commit s WIP). Ověřeno 5 min pod live zátěží: FD 14–42 stabilní, health 7–100 ms, public 200. POZOR pro Codex: Codexova dřívější diagnóza „NRestarts=0, externí restart" = můj restart přes SSH.
- **[Codex] Diagnóza krátkého výpadku webu — produkce read-only, bez změny kódu/dat** — proces `webos` vyčerpal file descriptory (`OSError: [Errno 24] Too many open files`) a přestal přijímat spojení; v 19:20:39 CEST byl externě restartován (systemd `NRestarts=0`) a znovu naběhl. Po restartu veřejný i origin health vrací 200, `app/db/maintenance` jsou OK/off a počet FD je stabilní (28–34 z limitu 524288). Příčina úniku FD zatím neurčena; restart obnovil provoz.
- **[Claude] FIX topbar nav přetékání (report od Matyho) — NASAZENO (cache 2026071244)** — `python deploy.py --deploy`: predeploy 489/489 + frontend OK, před deployem sha256 diff lokál vs `/opt/webos/app` potvrdil jediný rozdíl `web/styles.css`, restart/health/DB OK, CF Purge Everything OK. Živě ověřeno: cache 1244, produkční styles.css sha256 = lokál, nové breakpointy (1780/1620/1450) přítomné, starý `max-width: 1280px` 0 výskytů. — Maty hlásil, že nav lišta v shopu „chvíli je, chvíli není". Ověřeno Playwright měřením s reálným CSS: nav má od 14.7. **11 položek** (přibyla Podpora) = 859 px, ale breakpointy v `web/styles.css` byly laděné na 8 položek — plný topbar přihlášeného uživatele potřebuje ~1780 px, hamburger ale nastupoval až ≤1280 px. **V pásmu 1281–~1730 px topbar přetékal doprava** (zůstatek/profil oříznuté `overflow-x: clip`), kolem 1280 nav problikávala je/není. Fix: (1) media query skrývající lvl-pill/„sedláků"/jméno posunutá 1500→**1780 px**; (2) nový blok ≤**1620 px** zhušťuje nav-linky (padding 8/7, font 11.5, letter-spacing .01em → nav 682 px) — umístěný ZA v7 base `.nav-link` pravidly (pořadí zdroje, jinak ho přebijí); (3) hamburger breakpoint 1280→**1450 px**. Ověřeno headless Chromem na statickém harnessu i živé appce: overflow 0 na všech šířkách 1280–1920, nav viditelná ≥1451, hamburger ≤1450. Pozn. pro příště: **při přidání další nav položky přeměřit breakpointy** (komentář u media query). Druhá část Matyho reportu („přejíždí zleva doprava") = activity ticker pod topbarem, to je feature. Bez deploye. Pozn.: Playwright proti lokální appce visel na service workeru — řeší `service_workers="block"` v new_context.
- **[Codex] Ticket management restricted to admin role — DEPLOYED (cache 2026071243)** — `python deploy.py --deploy` shipped admin-only ticket management: all `/api/tickets/admin/*` endpoints require `require_admin`, broadcaster sees Support as a normal user and can access only their own tickets, while private Messages retain their existing admin+broadcaster permissions. Predeploy passed 489/489 plus frontend balance and `node --check`. Atomic Contabo release `webos-1784127285126043500`, restart/health/DB OK, maintenance off, Cloudflare Purge Everything OK. Live cache 1243 contains the admin-only frontend guard; deployed 180-file tree matches local hashes exactly (0 differences), protected admin inbox returns 401 without a session, and production support tables remain empty (0/0). No production data changed.
- **[Codex] Ticket management restricted to admin role — local, NOT DEPLOYED** — all `/api/tickets/admin/*` endpoints now require `require_admin`; broadcaster no longer receives the global unread count and sees Support as a normal user with only their own tickets. Frontend support management uses a dedicated admin-only permission check while private Messages keep their existing admin+broadcaster rules. Regression coverage verifies broadcaster gets 403 for inbox/read/reply/status actions. Targeted tests 2/2 and full `predeploy.py` 489/489 passed, including frontend balance and `node --check`. No production change or data intervention.
- **[Codex] Support helpdesk browser QA + production verification — DEPLOYED (existing cache 2026071242, no new release)** — Playwright completed the full desktop/mobile flow: create category → form → ticket → user reply → admin inbox/reply → resolved status, with no unexpected page errors or HTTP failures. Side-by-side reference comparisons and final evidence are recorded in `design-qa.md` (`final result: passed`). The requested support-only release was already active as `webos-1784125174788341100`, so it was not redeployed. Live verification: health/DB OK, maintenance off, cache 1242, separate Support and Messages frontend contracts present, `/api/tickets/mine` returns 401 without a session, production support tables exist and remain empty (0/0 rows). No production data changed.
- **[Codex] Zprávy a support tickety rozděleny + nový helpdesk UI — NASAZENO (cache 2026071242)** — `python deploy.py --deploy` nasadil samostatný helpdesk `#/podpora` s více tickety, kategoriemi, vyhledáváním, filtry, unread stavem, soukromým vláknem a řízením stavů; původní `✉️ Zprávy` jsou znovu jen týmová vlákna. Predeploy 489/489, frontend balance a `node --check` OK. Atomický Contabo release `webos-1784125174788341100`, restart/health/DB OK, maintenance off a Cloudflare Purge Everything OK. Produkční `app/` + `web/` jsou bitově shodné s lokálem; veřejně ověřena cache 1242, helpdesk UI, chráněné API (`/api/tickets/mine` → 401 bez session) a zachované přímé losování tomboly. Produkční tabulky `support_tickets` a `support_ticket_messages` vznikly prázdné (0/0 řádků).
- **[Codex] Tombola vrácena na přímé losování — NASAZENO (cache 2026071241)** — kvůli dalším rozpracovaným změnám vznikl čistý release z aktivní produkce obsahující pouze návrat tomboly: odstraněn commit/seed mezikrok, proof endpoint a `verify.html`; admin má znovu jediné tlačítko „Vylosovat výherce“ a backend losuje přímo přes `secure_choice`. Predeploy 487/487, frontend balance a `node --check` OK. Atomický Contabo release `webos-1784124890070793200`, restart/health/DB OK, maintenance off a Cloudflare Purge Everything OK. Produkční strom bitově odpovídá čistému release; veřejně ověřena cache 1241, přímé losování, nepřítomnost commit/verify toku, `verify.html` → 404 a nepřítomnost rozpracovaného `tickets.py`/nových ticket tabulek. Bez změny produkčních dat.
- **[Codex] Zprávy a support tickety rozděleny + nový helpdesk UI — lokálně, NENASAZENO** — `✉️ Zprávy` jsou vrácené k původnímu kontraktu (vlákno zahajuje tým, uživatel pouze odpovídá). Nová samostatná route `#/podpora` používá vlastní tabulky `support_tickets` + `support_ticket_messages`, dovoluje více ticketů na uživatele a má kategorie/podkategorie, předmět, popis, vyhledávání, filtry, unread badge, soukromý chat a stavy Otevřený → Řeší se → Vyřešený → Uzavřený. Admin/broadcaster vidí společný inbox a řídí stav; první odpověď týmu automaticky přepne ticket na „Řeší se“. UI vychází z dodaných helpdesk referencí, ale používá ZURYS oranžové tokeny, existující panely a responzivní desktop/mobile layout. Přílohy záměrně vynechány, dokud nebude určené bezpečné úložiště. Přidány oddělené regresní testy DM a ticket flow; `predeploy.py` PASS 489/489 + frontend syntax/balance. Lokální server běží na `http://127.0.0.1:8011/#/podpora`. In-app browser opakovaně timeoutoval při DOM/screenshot capture, proto je vizuální Design QA v `design-qa.md` správně označené `final result: blocked`; bez deploye a bez zásahu do prod dat.
- **[Codex] Tombola vrácena na přímé losování — lokálně, NENASAZENO** — odstraněn dvoukrokový commit/seed tok, provably-fair důkaz i stránka `verify.html`; admin má znovu jediné tlačítko „Vylosovat výherce“ a backend vybírá výherní tiket přímo přes `secure_choice`. Přidán regresní test losu bez commitu. Ověřeno cíleným testem, `node --check`, `git diff --check` a celým balíkem mimo známý pořadově závislý support-ticket test (vše ostatní prošlo; support test samostatně také prošel). Bez deploye a bez zásahu do produkčních dat.
- **[Codex] Support tickety na webu — NASAZENO (cache 2026071240)** — přihlášený uživatel může přes „🎫 Podpora“ sám založit požadavek; admin/broadcaster ho vidí jako nepřečtený v inboxu „Support tickety“ a odpoví ve stejném vlákně. Jedno vlákno na uživatele, limit 5 zpráv/min, bez nové tabulky a bez změny produkčních dat. Před deployem ověřeno, že ostatní změny pracovního stromu už odpovídají produkční crew release 1239. `python deploy.py --deploy`: 487/487 testů, frontend kontrola, atomický Contabo release `webos-1784123463239975100`, restart/health/DB OK, maintenance off, Cloudflare Purge Everything OK. Veřejný index a `app.js` potvrzují cache 1240 i oba support kontrakty; produkční SHA-256 `dm.py` a `app.js` jsou shodné s lokálem.
- **[Codex] Zpřehlednění crew žebříčku — NASAZENO (cache 2026071239)** — karta Týden nově jasně uvádí „Řazeno podle XP získaných tento týden“, zobrazuje týdenní `sub XP` místo matoucího all-time supporter součtu, vlastní partu zvýrazní štítkem „VAŠE PARTA“ a ukáže rozdíl do vyšší pozice. Supporteři jsou označeni jako supporter XP za celou historii; Měsíc jako supporter XP tento měsíc. Backend přidal `week_sub_xp = week_xp − week_farm`, pravidla řazení ani ekonomika se neměnily. Lokální UI QA: desktop 1280×720 + mobil 390×844 bez overflow, přepnutí Týden/Supporteři, čistá konzole a screenshoty; plný pracovní strom 487/487. Kvůli současně rozpracovaným support ticketům (NENASAZENO, překryv v `web/app.js`) vznikl čistý dočasný release bitově shodný s produkcí mimo přesně `app/crews.py`, `web/app.js`, `web/styles.css`; ostrý `python deploy.py --deploy` prošel 486/486, atomický restart/health/DB i Cloudflare purge. Živě ověřena cache 1239, nové kontrakty v HTTP/SSH a nepřítomnost support-ticket kódu v release; produkční data se neměnila.
- **[Codex] Support tickety na webu — lokálně, NENASAZENO** — existující soukromé vlákno bylo rozšířeno na jednoduchý support ticket: přihlášený uživatel může sám založit požadavek přes nově pojmenovanou sekci „🎫 Podpora“, admin/broadcaster ho vidí v inboxu „Support tickety“ jako nepřečtený a odpoví ve stejném vlákně. Zachováno jedno vlákno na uživatele bez nové tabulky; odesílání uživatele omezeno na 5 zpráv/min. Přidán end-to-end API test založení → admin inbox → odpověď → načtení uživatelem. Ověřeno plným pytest suite, `node --check web/app.js` a `git diff --check`; in-app browser poprvé načetl správný lokální web bez console chyb, další interakce timeoutovala. Bez deploye.
- **[Codex] Návrh zpřehlednění crew žebříčku — lokálně, rozhodnutí bez změny kódu** — karta Týden nemá vedle týdenního XP zobrazovat neoznačené all-time supporter XP, protože směšuje dvě období a působí jako chybné pořadí. Doporučeno ukázat týdenní sub XP (nebo jasně označit „celkem“), doplnit text „Řazeno podle XP tento týden“ a u vlastní party zobrazit rozdíl do vyšší pozice; Supporteři mají dál používat all-time sub XP.
- **[Codex] Ověření pořadí týdenního žebříčku part — lokálně + produkce read-only, bez změny kódu/dat** — pořadí je správně podle součtu aktuálního `week_xp` členů (`DESC`); Interatyho Klubík je #6 s 86 542 XP, zatímco pět part před ním má 91 469–215 063 XP. Zobrazených 🎁 240 000 je jiná metrika: all-time `sub_xp`, nikoli body pro týdenní pořadí. Produkční rozklad Interatyho Klubíku: 66 542 farm XP + 20 000 sub XP tento týden = 86 542; testy žebříčku 2/2 prošly.
- **[Codex] Ověřen maximální level zvířat na Statku — lokálně, bez změny kódu** — všechna zvířata mají společný strop `MAX_LEVEL = 10`; nový level získají každých 5 krmení, takže z levelu 1 dosáhnou levelu 10 po 45 krmeních.
- **[Claude] Crew: zrušen týdenní XP cap na člena — NASAZENO (cache 2026071238)** — na žádost uživatele („zruš v crew limity na XP"). `WEEK_XP_CAP = 10000` (max farm příspěvek 1 člena do crew XP za týden) v `app/crews.py` smazán; `contribute()` má teď `wk_add = farm_add = amount`, farm teče do crew XP celý stejně jako suby. `week_farm` sloupec ZŮSTÁVÁ — neslouží už capu, ale odvození `week_subs = week_xp − week_farm` (sub-cíl party by se bez něj rozbil). Bonus stropy `CREW_SUB_BONUS_CAP` (+40 %) / `CREW_FARM_BONUS_CAP` (+5 %) NETKNUTÉ — to jsou limity na sedláky z crew levelu, ne na XP. Testy: `test_contribute_capped` → `test_contribute_uncapped`, `test_sub_bypasses_weekly_farm_cap` → `test_sub_xp_tracked_apart_from_farm`; predeploy 486/486 + `node --check` PASS. `python deploy.py --deploy`: restart/health/DB OK, CF Purge Everything OK. Živě ověřeno přes SSH: `WEEK_XP_CAP` v `/opt/webos/app/app/crews.py` 0 výskytů, uncapped řádek na místě, health `ok`, maintenance `off`. Deploy odvezl i Codexovy necommitnuté frontend změny (garden timer scoping, SW reload) — ty už na produkci z jeho deploye 1237 byly, takže no-op.
  - **⚠️ Sleduj ekonomiku:** `GOAL_TIERS` (×1/×2/×4/×8, faucet max 2900 sedláků/člen/týden) byly laděné na to, že farm je capnutý na 10k/člen a vyšší tiery jdou jen s reálnými suby. Bez capu je jeden grinder přeteče sám → tier odměny budou padat výrazně častěji. Zároveň `CREW_LEVEL_BASE = 12000` počítal s ~67k/týden na 6člennou partu (lvl 10 ≈ 3,5 měsíce) → levely poletí rychleji. Když to ujede: zvednout `GOAL_PER_MEMBER` nebo `CREW_LEVEL_BASE`. Revert capu = vrátit konstantu + `farm_add = max(0, min(WEEK_XP_CAP - cur_farm, amount)); wk_add = farm_add`.
- **[Claude] jan_matu (311) unlimited sázky + hazard (prod-data)** — na žádost uživatele udělen plný unlimited: uid 311 přidán do `eco_wager_exempt_uids` (`[631,433,675,504,311]`) i `eco_games_exempt_uids` (`[433,675,311]`) a jeho osobní `users.wager_limit` 300000 → 0 (exempt ho jinak přebíjí přes `min()`). Živé settingy, bez deploye. Zbývá jen globální `DUEL_MAX` 500k/duel. Revert: smazat 311 z obou listů + `wager_limit=300000`.
- **[Codex] FIX zrychleného odpočtu na Statku — NASAZENO (cache 2026071237)** — zahradní `_gardenTimer` je v `web/app.js` omezený na svůj `gardenBox`, takže po návštěvě Zahrádky už neodečítá farmářské `.grd-time` podruhé. Statický test v `tests/test_garden.py` upraven na scoped kontrakt. Predeploy 486/486 + frontend syntax PASS; před deployem porovnáno 180 nasazovaných souborů s aktivní produkcí, jediný rozdíl byl zamýšlený `web/app.js`. `python deploy.py --deploy` nasadil atomický Contabo release `webos-1784067802056740200`, restart/health/DB OK a Cloudflare Purge Everything OK. Živě ověřeno: index i SW cache 1237, nový selektor přítomen, starý globální selektor nepřítomen, produkční SHA-256 `app.js` shodný s lokálem (`da608fd...01213`), maintenance `off`.
- **[Codex] FIX zrychleného odpočtu na Statku — lokálně, NENASAZENO** — v `web/app.js` jsou oba dotazy zahradního `_gardenTimer` omezené na jeho existující `gardenBox` (`box.querySelectorAll`) místo globálního `document`. Interval ponechaný po odchodu ze Zahrádky už proto neodečítá časovače Statku podruhé ani nezasahuje jiné stránky. Ověřeno `node --check web/app.js`, `git diff --check` a kontrolou, že globální selektor `.grd-time` zmizel. Bez cache bumpu a bez deploye.
- **[Codex] Diagnóza zrychleného odpočtu na Statku — lokálně, bez změny kódu** — backend počítá `ready_at` korektně podle UTC a produkčních hodin zvířete; chyba je pouze ve frontendu po předchozí návštěvě Zahrádky. Její `_gardenTimer` po změně stránky zůstává aktivní a každou sekundu odečítá globálně všechny `.grd-time`, zatímco Statek spustí vlastní `_farmTimer` nad stejnými prvky. Zobrazený odpočet proto běží 2× rychleji; serverový čas se nemění a produkt nelze sebrat předčasně. Minimální oprava: omezit dva zahradní selektory v `web/app.js` na `#gardenBox` (nebo timer při změně routy rušit). Bez deploye.

## 2026-07-14

- **[Codex] Produkční QA PWA cache hotfixu — PASS, maintenance ponechána ON** — veřejný nepřihlášený browser správně dostal maintenance 503; prod DB/audit potvrdil, že ji ručně zapnul `Interaty` (uid 12) v `2026-07-14T19:33:26Z`, bez odpočtu, nikoli hotfix/deploy. Přes existující admin Chrome session (admin maintenance bypass) ověřen skutečný `/#/statek`: `app.js`, `styles.css` a `farm.css` všechny `2026071236`; 10/10 aktuálně renderovaných farm/shop WebP mělo `complete=true` a nenulové přirozené rozměry, 0 broken images, 0 `.stk-atlas*` markupů, scéna i data farmy vykresleny, console bez warning/error. Bezpečná interakce „Jak funguje Statek?“ otevřela správný návod; po plném reloadu zůstalo 10/10 obrázků, 0 atlasů/rozbitých obrázků a jednotná verze 1236. Screenshot vizuálně potvrdil nové samostatné sprity. Maintenance nebyla měněna; bez explicitního pokynu zůstává zapnutá.
- **[Codex] HOTFIX Statek/PWA cache — full client restart — NASAZENO (cache 2026071236)** — příčina atlasů a SVG u části lidí byla smíchaná generace PWA shellu (starý `app.js` + nová `farm.css`): nový worker sice `skipWaiting()` + `clients.claim()` aktivoval, ale už otevřený dokument nereloadnul, takže v paměti zůstal starý frontend. `web/sw.js` teď po smazání starých `zurys-shell-*` cache převezme i nekontrolovaná otevřená okna a jednou provede `client.navigate(client.url)`; každý další SW/cache bump tedy sjednotí HTML+JS+CSS automatickým reloadem. `deploy.py --selftest` nově hlídá pořadí delete → claim → matchAll a přítomnost navigate. Před deployem: `node --check` SW+app, selftest, 486/486 testů; 180 nasazovaných souborů porovnáno SHA-256 s aktivní produkcí, jediný rozdíl byl zamýšlený `web/sw.js`. `python deploy.py --deploy`: atomický Contabo release `webos-1784057101211613700`, cache 1235→1236, restart/health/DB OK, Cloudflare Purge Everything OK. Browser při aktivaci nového workeru skutečně ztratil kartu kvůli vynucené navigaci (očekávaný restart klienta); finální veřejný kontrakt ověřen: jednotná verze 1236 v index/SW/app, SW obsahuje claim+matchAll+navigate, app registruje `sw.js?v=2026071236`, starý atlas 404, aktivní SW SHA-256 = lokál, 34/34 animal assetů = lokální SHA-256, health `ok`, DB `ok`, maintenance `off`.
- **[Codex] Diagnóza zobrazení celého atlasu místo zvířat — lokálně, bez změny kódu** — screenshot odpovídá cache mismatchi: starý atlasový `app.js` vytváří `statek-atlas.webp`, ale novější `farm.css` už nemá `.stk-atlas-*` crop pravidla, takže se celý atlas zmenší do každého slotu i shop karty. Aktuální produkce ověřena přes HTTP i browser: starý atlas vrací 404, `app.js`/`farm.css` neobsahují `statek-atlas`, `FARM_ATLAS_BOX` ani `.stk-atlas-*`; aktuální samostatný WebP se dekóduje 240×143 bez console chyb. Jde o historický PWA/service-worker/browser cache shell u otevřených klientů; tvrdý refresh nebo smazání dat webu ho odstraní.
- **[Codex] Produkční QA Statku po rozdílném artu — lokálně, bez změny kódu** — PASS: celý pytest suite 486/486, `node --check`, `deploy.py --selftest`, health `ok`, jednotná cache verze `2026071235`, aktuální WebP→SVG fallback kód a žádný starý atlas kód. Všech 34 produkčních animal assetů (17 WebP + 17 SVG) vrací HTTP 200, správný MIME a SHA-256 shodný s lokálem. Produkční shell `/#/statek` se vykreslil bez console warning/error, navigace do Shopu fungovala; přímý WebP se před i po reloadu dekódoval jako 240×143 bez chyb. Screenshot API in-app browseru dvakrát timeoutovalo (známý problém), DOM/decode kontrola prošla. Přihlášený per-user farm flow nebyl v testovacím browseru dostupný; jeho logiku pokrývá test suite.
- **[Codex] Diagnóza rozdílného artu Statku — lokálně, bez změny kódu** — první screenshot renderuje aktuální WebP sprity, druhý staré stavové SVG, na které `stkFixImgs()` záměrně přepne po chybě WebP (případně je má už otevřená stará verze JS). Produkce aktuálně vrací oba assety jako HTTP 200, takže nejde o chybějící produkční soubor. Rozdílný počet zvířat/slotů a časy jsou per-user stav z DB. Stačí tvrdý refresh nebo zavřít starou kartu; pokud WebP dál padá, rozhodnou Network/Console data z jeho prohlížeče.
- **[Claude] FIX statek: hnědý flek pod krávou — NASAZENO (cache 2026071235)** — uživatel hlásil „statek buguje", na zoomu měla kráva pod sebou kus cizí hnědé grafiky. Příčina: `stkImg()` krájelo zvířata za běhu obdélníkovým ořezem z `statek-atlas.webp` a souřadnice v `FARM_ATLAS_BOX` byly ručně odhadnuté — ověřením proti alfa masce atlasu vyšlo, že **všech 17 boxů sedělo špatně** (uřezané nohy, useknutý mlékárenský konev/vejce, zatažený soused). Hlubší problém: v atlasu se kráva-ready a kůň-ready **fyzicky dotýkají** (koňská hříva × kravské kopyto = jedna souvislá komponenta i při alfa>180), takže žádný obdélník je nerozdělí — proto ten flek. Řešení = přestat krájet za běhu: nový `design/statek-art/split_atlas.py` udělá ze `statek-atlas-c-key.png` (chroma zdroj, v gitu) 17 samostatných WebP spritů — chroma klíč s despillem → scanline union-find CC → každý sprite maskovaný **vlastní komponentou** (cizí pixely odalfované), jedno slepené místo dělí ručně zadaný `SEAM`. Frontend se zjednodušil: `FARM_ATLAS_BOX` + crop matika + `.stk-atlas-frame/-crop/-source` CSS + dvouvětvý `stkFixImgs` **smazány**, `stkImg()` je teď jeden `<img src="{art}-{stav}.webp">` s fallbackem WebP → SVG → emoji (`data-f` mířilo na `{art}.webp`, které nikdy neexistovalo = mrtvá větev, opraveno na stavové SVG). `web/img/farm/animals/statek-atlas.webp` smazán (166 KB, už není potřeba; 17 spritů = 176 KB). Ověřeno: 17/17 spritů zkontrolováno na fialovém pozadí (žádný zelený lem, žádný oříznutý kus, kráva-ready čistá), predeploy 486/486 + `node --check`, živě na dev serveru se všech 6 spritů ve scéně načte se správnými rozměry (cow-ready 270×149) a správným stavovým mapováním (hungry/growing→producing/ready, unicorn→horse, horse→dog), 0 fallbacků na SVG/emoji. Pozn.: `computer{screenshot}` v preview opakovaně timeoutoval (známé z minula) → vizuální důkaz vytažen přes canvas snapshot načtených `<img>`. Deploy: před ostrým během ověřeno sha256 lokál vs `/opt/webos/app` soubor po souboru — strom byl identický s produkcí až na `app.js`+`farm.css`, takže nic cizího neuteklo; `deploy.py --deploy` (predeploy 486/486, health 200, CF purge). Po deployi ověřeno živě: všech 17 spritů `200 image/webp` se shodným sha256 proti lokálu, starý `statek-atlas.webp` → 404, `FARM_ATLAS_BOX`/`stk-atlas` už nejsou v živém app.js ani farm.css.
- **[Claude] OPRAVA ZÁZNAMU: provably-fair tombola JE NA PRODUKCI (od cache 2026071234)** — záznam z 13. 7. ji vede jako „lokálně, NENASAZENO", ale to už neplatí a je to zavádějící. `deploy.py` tar-uje **celý pracovní strom** (`app/ web/ requirements.txt`), takže deploy „Mobilní/perf optimalizace" ji odvezl s sebou. Ověřeno na serveru: `raffle_draws`/`seed_hash` v `/opt/webos/app/app/routers/admin.py` (15×) i `shop.py` (3×), `web/verify.html` nasazený. Poučení pro příště: `deploy.py --deploy` **nikdy nenasadí jen jednu featuru** — co je v pracovním stromu, to jde na produkci. Kdo chce vyloučit rozdělanou práci, musí jít cestou čistého release z produkce (viz starší záznamy Codexu) nebo si strom předtím uklidit. Před deployem se vyplatí sha256 diff lokál vs `/opt/webos/app`.
- **[Claude] Mobilní/perf optimalizace — NASAZENO (cache 2026071234)** — (1) `web/img/garden` sprite atlasy `garden-crops.png`+`garden-decor.png` (3.1MB) smazány, nikde neodkazované, zdroj v `design/garden-art/`; (2) reálně používané `garden/crops/*`, `garden/decor/*` a `farm/animals/statek-atlas.png` převedeny PNG→WebP (3,9MB→0,8MB, ~78 %), `gardenImg()`+`stkImg()` v `app.js` přepnuty na `.webp`, staré PNG smazány; (3) `mimetypes.add_type("image/webp",".webp")` v `app/main.py` — bez toho StaticFiles servírovalo webp jako `application/octet-stream` (Windows mimetypes DB neznala webp; s `X-Content-Type-Options: nosniff` riziko že prohlížeč obrázek odmítne), týkalo se i staršího `garden-hero.webp`; (4) `GZipMiddleware` přidán do `app/main.py` — origin teď gzipuje `app.js`/`styles.css` (552K/196K nekomprimovaně předtím), ověřeno `content-encoding: gzip` živě přes CF; (5) `.input`/`.select`/textarea font-size 14.5px→16px v `styles.css` — iOS Safari auto-zoom při focusu na pole <16px (login/redeem/sázky/chat). Ověřeno: 486/486 testů, `node --check`, `deploy.py --deploy` (predeploy+health+CF purge OK), živé fetch na zurys.live potvrdily `image/webp` + `gzip` na nových cestách. Cache bump v `web/index.html` NENÍ commitnutý (deploy.py to nedělá záměrně).
- **[Claude] Martech1 (433) unlimited sázky obnoveny (prod-data)** — byl v obou exempt listech (eco_wager/games_exempt_uids), ale měl osobní `wager_limit=100000` který exempt přebíjí → nastaveno zpět na 0. Živé settingy, bez deploye. Pozn.: jediný zbývající strop je DUEL_MAX 500k/duel (globální konstanta).
- **[Claude] CaseHug undo (mazání historie) — NASAZENO (release webos-chundo, cache 2026071235)** — čistý release z produkce + undo změny na PROD verze admin.py/models.py/app.js (tombola dál vyloučena), cache bump + CF Purge Everything, health 200 + DB ok, prod ověřen (app.js ch-undo, admin.py /casehug/undo). — nový `POST /admin/casehug/undo {log_id}`: vrátí sedláky i XP (clamp na 0, kdyby už utratil), smaže řádek points_logu → uvolní deposit ID pro správné připsání; audit `casehug.undo`. UI: 🗑️ tlačítko u každého řádku historie + confirm. Nový model CasehugUndoIn. Test: award → undo → zůstatek/XP zpět, řádek pryč, stejné deposit ID jde znovu, 404 na cizí log_id (7/7 pass). Browser QA na dev DB: tlačítko renderuje, undo přes UI flow vrací body (pozn.: lokální SW cache drží starý app.js podle ?v= — na produ řeší cache bump).
- **[Claude] CaseHug vlastní částka — NASAZENO (release webos-casehug, cache 2026071234)** — nasazeno jako čistý release: kopie aktivního prod release + casehug změny aplikované na PROD verze admin.py/app.js (tombola dál vyloučena) + cache bump index.html/sw.js/app.js a CF Purge Everything. Health 200, DB ok, prod app.js obsahuje ch-award-custom, admin.py CASEHUG_RATE. — award endpoint už nevyžaduje preset: přijme celá eura 1–500 a dopočítá kurz 100 sedláků + 100 XP / € (`CASEHUG_RATE`, `CASEHUG_MAX_EUR` v admin.py; presety zůstávají jako UI tlačítka). Admin UI (app.js) má pod presety pole „Vlastní částka €" + živý dopočet (+N 🌾 · +N XP) + tlačítko Připsat; dedup/deposit-ID logika beze změny. Test upraven: 7 € → 200 (+700/+700), 0 a 501 → 400. Ověřeno: pytest test_casehug 6/6, node --check, browser QA na dev DB (input + dopočet + bez console chyb).
- **[Claude] Zrušen anti-farma IP/zařízení check u PvP her a duelů — NASAZENO (release webos-ipfix, bez cache bumpu)** — na žádost uživatele (hráči se stejnou IP se reálně neznají, check blokoval legitimní duely) odstraněn `_same_person` + obě kontroly v joinu piškvorek a duelů (`app/routers/games.py`); anti-farma check u posílání bodů (misc.py) a dropů ZŮSTÁVÁ. 485 testů prošlo. Nasazeno jako čistý release: kopie aktivního produkčního release + jen games.py (provably-fair tombola zůstala vyloučena), ověřen sha256 prod == lokál, atomický symlink switch s rollbackem, health 200 + DB ok. Backend-only → žádný cache bump ani CF purge. Změna je jen v pracovním stromu, necommitnuto.
- **[Codex] Statek + Zahrádka — mobilní optimalizace — NASAZENO (cache 2026071233)** — čistý release z aktuální produkce obsahoval pouze nové mobilní CSS; lokální provably-fair tombola zůstala vyloučena. Predeploy: 485 testů, JS parser a CSS kontrola OK; atomický restart, health 200, DB `ok`, maintenance `off`, Cloudflare purge a veřejné SHA-256 obou CSS shodné. Post-deploy Browser QA na 360/390 px: bez horizontálního overflow a console chyb; Statek má scénu o ~187 px výš a stránku o 273 px kratší, Zahrádka je o 382 px kratší, semínka 2×2, dekorace `nowrap`, prodej zvířete viditelný na dotyku; výběr semínka funguje.
- **[Codex] Statek + Zahrádka — mobilní optimalizace — lokálně, NENASAZENO** — na breakpointu ≤720 px zhuštěn header a stavové panely Statku, scéna posunuta výš, prodej zvířete je nově dostupný i bez hoveru a dotyková turbo akce zvětšena. Na ≤460 px je Zahrádka kratší, semínka jsou ve dvou sloupcích, dekorace se vodorovně posouvají bez zalamování a akční prvky mají větší dotykovou plochu; varianta 5–8 záhonů byla zkrácena bez změny herní logiky. Produkční baseline změřena na 360/390 px bez horizontálního overflow, výběr semínka a návod Statku fungují bez console chyb. Lokální post-change snapshot prohlížeč bezpečnostně odmítl načíst; statická CSS kontrola, `git diff --check`, JS parser a všech 485 testů prošly.
- **[Codex] Produktový vizuál PS5 sladěný se zurys.live — lokálně** — schválen směr tmavého navy pozadí s oranžovým akcentem a jemným glow podle aktuálních CSS tokenů webu; konzole a ovladač zůstávají bez reklamního textu a vodoznaku.
- **[Codex] Produktový vizuál PlayStation 5 Slim Digital Edition — lokálně** — zvolen čistý čtvercový e-shopový produktový snímek konzole s ovladačem, bez ceny, reklamního textu a vodoznaku; generování přes ImageGen.
- **[Codex] Zahrádka — malovaný redesign — NASAZENO (cache 2026071232)** — z aktuálního produkčního release vznikl čistý atomický release doplněný pouze o novou Zahrádku; lokální provably-fair tombola byla záměrně vyloučena. Predeploy prošel 485 testy, JS parserem a CSS kontrolou; `webos.service` naběhl, Cloudflare cache byla promazána. Veřejný health: 200, DB `ok`, maintenance `off`; hero, plodina a dekorace ověřeny shodným SHA-256 proti release.
- **[Codex] Zahrádka — malovaný redesign podle schválené varianty — lokálně, NENASAZENO** — původní kartičkový přehled nahrazen živou golden-hour scénou s reálnými PNG/WebP assety pro plodiny, růstové/sklizňové stavy, škůdce, hnojivo, dekorace a budovy. UI používá skutečná data a zachovává herní ekonomiku; podporuje 4–8 záhonů, sklizeň všeho, výběr semínka, sázení, dekorace i mobilní dvousloupcové rozložení bez přetečení. Browser QA proti schválenému mocku prošlo bez console chyb (`design-qa.md`: passed); ověřeno `node --check`, plným `pytest -q` a `git diff --check`.
- **[Claude] Provably-fair tombola (commit-reveal) — lokálně, NENASAZENO** — los tomboly převeden z „věř adminovi" na ověřitelný. Nová tabulka `raffle_draws` (seed, seed_hash, roster, roster_hash, winner_index) — `raffle_winners` beze změny, takže feed/profil/achievementy jedou dál. Dvoufáze v `admin.py`: `POST /raffle/{id}/commit` zamkne tajný seed (zveřejní `sha256(seed)` + `roster_hash` PŘED losem → admin nemůže ohnout výsledek) a `POST .../draw` odhalí seed a spočítá výherce deterministicky `winner = int(HMAC_SHA256(seed, roster)[:8]) % total`. `undo-draw` maže výherce i commit. Veřejný `GET /shop/raffle/{id}/proof` + nová stránka `web/verify.html?pid=` přepočítá výsledek v prohlížeči přes SubtleCrypto a ukáže ✓/✗. Admin UI (app.js) teď dvoukrokové (🔒 Commit → 🎲 Odhalit&vylosovat) + odkaz 🔍 Ověřit. `secure_choice` už v admin.py nepoužit (odebrán z importu). Ověřeno lokálně: self-check algoritmu (determinismus + uniformní rozdělení 5000 losů/139 pozic), plný flow na dev DB (12 tiketů) — verify stránka 4× zelená; tamper test (přepis winner_index v DB) → verdikt ✗ NESEDÍ (index check odhalil rig). Dev data uklizena.
- **[Codex] Statek art C + nové pozadí — NASAZENO (cache 2026071231)** — `python deploy.py --deploy` nasadil atomický release na Contabo, restartoval `webos.service` a provedl Cloudflare Purge Everything. Predeploy: 485 testů, JS syntax a frontend balance OK. Produkční health 200 (`maintenance=off`, DB OK); veřejný atlas a hero WebP mají shodný SHA-256 s lokálními soubory, produkční app.js/CSS obsahují atlas, nové pozadí, lišku i mobilní layout.
- **[Codex] Statek art C + nové pozadí — lokálně, NENASAZENO** — vygenerován a integrován nový cozy/realistický set zvířat jako průhledný atlas `web/img/farm/animals/statek-atlas.png` a nové golden-hour pozadí `web/img/farm/statek-hero-c.webp`; zdrojové koncepty a chroma podklady jsou v `design/statek-art/`. `stkImg()` ořezává stavové pózy hungry/producing/ready z atlasu, shop používá producing, hlad dál desaturuje CSS, pending fox přidává lišku do scény. Zachován fallback atlas → stavové SVG → původní WebP → emoji. Rozložení statku upraveno pro více slotů a mobil bez clippingu. Ověřeno desktop/mobile browser QA bez přetečení a console chyb, fallback testem, `node --check`, generátorovým self-checkem a `tests/test_farm.py` 19/19.

## 2026-07-13

- **[Claude] Fly.io zurys-shop ZRUŠEN (prod-infra)** — příčina „flash bonus občas nejde vybrat": Fly fallback běžel dál s vlastní zamrzlou DB, jeho partners_flash daemon + kickbot hlásily do chatu fantom flash kola, která na Contabu neexistovala. Postup: pflash_enabled=0 na Fly → finální DB záloha `C:\Users\Administrator\webos-backups\fly-app-db-final-2026-07-13.db` (117 MB, integrity ok, 1893 users) → `flyctl apps destroy zurys-shop` (app + volume pryč). Contabo ověřen zdravý, flash tam jede. AGENTS.md aktualizován (Fly neexistuje, flyctl nepoužívat), `fly.toml` smazán z working tree (necommitnuto). Ruční test: flash kolo force-open na produ přes SSH (`WEBOS_DATA_DIR=/data` nutné, env file ho nemá) — oznámení + web OK.
- **[Codex] Statek art set styl B integrován — lokálně, NENASAZENO** — z `design/statek-art/gen_animals_b.py` se deterministicky exportuje 17 těsně oříznutých SVG do `web/img/farm/animals/` (5 producentů × hungry/producing/ready + pes + liška), bez karty, ground stínu, desaturace a zzz. `stkImg()` používá stavové SVG s fallbackem SVG → původní WebP → emoji, `growing` mapuje na `producing`, shop používá `producing`; pending fox event přidává do scény samostatnou lišku. Ověřeno generátorovým self-checkem, `node --check`, browser QA bez clippingu/console chyb a `tests/test_farm.py` 19/19.
- **[Claude] Statek art set styl B - PODKLADY pro Codex (design/statek-art/)** - uzivatel schvalil styl B (mekka cozy ilustrace); vygenerovan kompletni SVG set: 5 producentu x 3 stavy (hladove lezi+spi / vyrabi / hotovo s produktem a zari) + pes hlidac + liska. Zdroj tvaru = gen_animals_b.py (funkce per zvire), integracni plan v design/statek-art/README.md (export per-asset SVG do web/img/farm/animals/, stavovy stkImg s fallbackem na .webp/emoji). Integraci stavi Codex.

- **[Claude] FIX neviditelny countdown v idle CTA statku + cache bump dira — NASAZENO (cache 2026071230, commit 4fc5f52)** — divaci videli "Slepice doda vejce za" bez casu a zlate tlacitko: (1) globalni .grd-time ma accent barvu = zlata na zlatem -> inline color:inherit; (2) bump_version nahrazoval jen aktualni verzi -> farm.css pridana pozdeji visela na ?v=2026071018 a prohlizece+SW shell drzely starou CSS; bump ted regexem sjednoti VSECHNY ?v= (index.html, sw.js, app.js), selftest rozsiren. Overeno: prod index+sw.js jednotne 2026071230, lokalne po SW updatu tmava idle lista s tikajicim casem.

- **[Claude] Statek UX batch (nakrmit vse / stavy zvirat / nejblizsi udalost) — NASAZENO 13.7. (cache 2026071229, commit 99b5ebc)** — backend farm.feed_all + POST /farm/feed-all (krmivo prednostne, preskoci nedostatek; test pridan, 19/19 pass). Frontend: zelene CTA Nakrmit vse (N× · cena) pod scenou (>=2 hladova; odhad ceny pocita krmivo zdarma), stavove tridy .is-hungry (ztlumene + spici zzz) / .is-growing / .is-ready (glow) / .is-fox, idle lista misto Zatim neni co sebrat ukazuje nejblizsi produkt (Slepice doda vejce za 1h 59m, zivy countdown). Soubory: app/farm.py, app/routers/misc.py, web/app.js, web/farm.css, tests/test_farm.py. Overeno v prohlizeci na dev DB, predeploy OK.

- **[Codex] Synchronizace nasazeného stavu do `origin/main` — NASAZENO** — ekonomická pojistka a reporting, bezpečnostní opravy OAuth/webpush, regresní testy, UI polish a subgoal overlay včetně crest SVG. Všechny změněné soubory z `app/` a `web/` ověřeny SHA-256 proti aktivní Contabo release; pouze commit + push, bez deploye.
- **[Claude] AGENTS.md + WORKLOG.md** — pravidla pro Kick webhook response kódy + tenhle sdílený deník. Lokálně, untracked.
- **[Claude] Kompenzace GROOF890 (prod DB, 19:00 UTC)** — ztracený gift z 503 stormu: +1080 (`Kick gift sub 🎁 ×1 (kompenzace 503 storm 13.7.)`), record_gifter + subgoal.tick(1), +1 spin. Turbo žeton nepřidán (měl 3/3 z ručního grantu 18:02 UTC — kdo?). Kick leaderboard ověřen: chyběl přesně 1 gift; user rozhodl žetony navíc nechat.
- **[Claude] FIX webhook 503 storm — NASAZENO (~18:52 UTC, cache 2026071228)** — commit `8401852` vracel Kicku 503 i po úspěšném zpracování → retry storm, pomalý web. Fix: `return Response(200)` v success větvi `app/routers/kickhook.py`. Ověřeno: 100 % webhooků 200, load klesl. Fix + cache bump zatím NENACOMMITNUTÉ.
- **[Codex] Jednorázový backfill turbo žetonů (prod DB, 18:02 UTC) — prod-data** — nebyl to individuální grant. Doplnil jsem platné gift suby zaznamenané před spuštěním turbo funkce 10. 7. 18:47:58 UTC, ještě v sedmidenní platnosti a s limitem 3 žetony na účet: 147 žetonů pro 131 účtů. GROOF890 měl odpovídající historické gifty, proto dostal 3/3. Před zásahem vznikla ověřená SQLite záloha; transakce měla idempotentní marker `_turbo_prelaunch_backfill_20260713` a kontrolu, že nikdo nepřekročil limit.

## 2026-07-14 — Claude: backfill tiketů „10€ GIFT CARD na CaseHug" (product 122)
Owner založil produkt jako typ `short` → 37 ze 44 nákupů nedostalo tiket do `raffle_entries` (typ mezitím přehozen na `raffle`, posledních 7 už tikety mělo). Doplněno na prod DB per-user dorovnáním orders→entries: +37 řádků (5 uživatelů), teď 44 orders = 44 tiketů. Rollback: `DELETE FROM raffle_entries WHERE product_id=122 AND created_at='2026-07-14T14:19:51.187630+00:00'`. Žádné vracení bodů potřeba.
- **[Claude] Tombola CaseHug 10€ GIFT CARD (produkt 126) — backfill 196 chybějících tiketů (17.7. ~18:10 UTC, jen prod data, bez deploye)** — produkt byl založen jako normální typ a na `raffle` přepnut až ~17:14 UTC; nákupy před přepnutím vytvořily `orders`, ale ne `raffle_entries` (17 uživatelů, největší WelIxd ×133). Doplněno přímo v prod DB: za každou objednávku bez tiketu INSERT do `raffle_entries` s původním `created_at` objednávky. Ověřeno: orders 545 = entries 545, 0 uživatelů s rozdílem. Rollback: `DELETE FROM raffle_entries WHERE id IN (...)` — ID vložených řádků v `/root/raffle_backfill_126_20260717.json` na Contabu.
- **[Claude] FIX odznak Šťastlivec se ztrácel (raffle_winners CASCADE) — prod DB opraveno 17.7., kód lokálně (commit níže), bez deploye** — ticket matejlus007 (výhra 15.7., produkt 124, odznak nepřišel): `raffle_winners.product_id` mělo FK `ON DELETE CASCADE`, takže smazání dolosované tomboly ze shopu smazalo i záznam výherce; když se produkt smazal dřív, než achievements daemon (10min sken) stihl projít, odznak `lucky` se nikdy neudělil. Tabulka na produ byla úplně prázdná. Oprava na prod DB (záloha `/data/backups/pre-rafflewin-fix-20260717.db`): tabulka přestavěna bez FK na products (user FK cascade zůstal), výherci zrekonstruováni replayem `admin_audit` (draw/undo v pořadí) → 63 řádků obnoveno, 13 chybějících odznaků `lucky` doplněno s datem výhry (vč. matejlus007); nedohledatelní: KebabFC2, KarelL1skovec, filipeeklol (smazané/přejmenované účty). V kódu: `db.py` SCHEMA bez FK + migrace `_mig_rafflewin_keep_history` (na produ flag už nastaven = přeskočí se; platí pro čerstvé DB). Selfcheck: migrace na staré schéma → FK pryč, výherce přežil smazání produktu. Uživateli v ticketu odpovězeno, že odznak je doplněn.
- **[Claude] FIX mobilni menu nescrollovalo (admin dole nedosazitelny) — NASAZENO 18.7. (cache 2026071259, commity eeec194+2482523)** — `.mobile-nav` je position:fixed bez limitu vysky; kdyz je polozek vic nez vyska okna (admin ucet), spodek pretekl mimo obrazovku a scroll stranky s fixed prvkem nehne. Fix 1 radek v styles.css: `max-height: calc(100dvh - var(--header-h)); overflow-y: auto`. Overeno lokalne (375x550: 636px obsahu v 486px okne, scroll dojede na posledni polozku) i na proda (styles.css?v=2026071259 fix obsahuje). POZOR pri deployi: v pracovnim stromu byla necommitnuta zmena app/economy.py (`return 1.0` = vypnuta inflacni brzda soft faucetu, bez WORKLOG zaznamu) ktera shazovala test_soft_faucet_guard_halves_rewards a byla by potichu odjela na prod (prod ji nema); pro deploy docasne odstranena, po deployi ji uzivatel/Codex vratil do stromu — pristi deploy ji odveze a predeploy test spadne, nekdo musi rozhodnout: bud smazat radek, nebo upravit test + zapsat do WORKLOGu.
- **[Claude] Inflační brzda soft faucetu VYPNUTA — NASAZENO 18.7. (cache 2026071260)** — na přání ownera („žádné limity na dropy"): `soft_faucet_factor()` v `app/economy.py` vrací natvrdo `return 1.0` (první řádek fce). Efekt: dropy, kolo štěstí i partner bonusy dávají VŽDY plnou odměnu; hláška „⚖️ Ekonomická pojistka je aktivní" (`award["guarded"]`) už nikdy nevyskočí. Vyřešuje otevřenou otázku z předchozího zápisu (necommitnutý `return 1.0` visel ve stromu bez rozhodnutí) — rozhodnutí = brzdu zrušit. Test `test_soft_faucet_guard_halves_rewards` přejmenován na `test_soft_faucet_guard_disabled` a přepsán na plnou odměnu (amount 100, guarded False). Predeploy 496 pass, health 200, CF purge OK. Revert = smazat řádek `return 1.0` + vrátit test.
- **[Claude] Statek: tvrdší leveling zvířat — NASAZENO 18.7. (cache 2026071261, commit 0e5187c)** — na přání ownera (zejdyn hlásil max lvl za ~týden). `app/farm.py`: `MAX_LEVEL 10→25`, `FEED_PER_LEVEL 5→20` → max = **480 krmení** (bylo 45, ~10× víc). Aby 25 levelů nenafouklo faucet: `LEVEL_REWARD_STEP 0.10→0.04` a `LEVEL_SPEED_STEP 0.03→0.015` → lvl25 = +96 % výnosu / cyklus 64 % ≈ starý top lvl10 (+90 % / 73 %). **Živý přepočet**: level = `1 + fed_count//20`, takže po deployi každému přepočte level z uloženého `fed_count` — kdo měl < 480 nakrmení spadne níž a dohrává (přesně ten úbytek „hned na max"), heavy krmiči (fed 480+) zůstanou nahoře. Frontend bere `max_level` z API (dynamic), zadrátovaná byla jen nápověda v `web/app.js` (⭐ „Leveluj do 25 / dvacet krmení"). Predeploy 496 pass, health 200, CF purge OK. Revert = 4 konstanty farm.py:52-55 zpět na 5/10/0.10/0.03 + hláška app.js.
- **[Claude] Statek: grandfathering levelů po zpřísnění — NASAZENO 18.7. (cache 2026071262)** — follow-up předchozího záznamu: přepočet `FEED_PER_LEVEL 5→20` srazil levely i STÁVAJÍCÍM hráčům (59 zvířat z max lvl 10 spadlo na ~5) — owner chtěl staré levely vrátit, tvrdý grind jen pro DALŠÍ postup. Migrace `_mig_farm_fed_rescale` v db.py: `fed_count = (min(fed,45)//5)*20 + (min(fed,45)%5)*4` na `farm_animals` + `farm_collection` (stáj) → starý level (cap 10) i rozkrmený zbytek sedí 1:1, další level = 20 krmení, strop 25. Ověřeno na produ: flag '1', přesně 59 zvířat zpět na lvl 10, max fed 180 (nikdo nedostal víc než měl). Drobnost: krmení mezi 14:52–15:05 (mezi deployi) se přeškálovalo ×4 taky — jednotky krmení, zanedbatelné.
- **[Claude] Podpora: admin batch (čeká-filtr, interní poznámky, historie ticketů, pohyby bodů inline) — lokálně, NENASAZENO** — 4 vylepšení admin pohledu Podpory: (1) seznam ticketů nese `last_from_user` (SQL subselect v `/tickets/admin/all`) → ⏳ badge u ticketů čekajících na odpověď podpory + filtr „⏳ Čeká (N)" v sidebaru (jen staff); (2) interní poznámky — `POST /tickets/admin/{id}/note` zapisuje event `note` do `support_ticket_events`, uživateli je `_events()` nikdy nevrací (filtr v SQL), admin UI má žlutý blok 📝 s composerem, poznámky vyfiltrované z historie ticketu; (3) user_ctx nese posledních 5 ticketů uživatele (id/předmět/stav) → klikatelná sekce „Předchozí tickety"; (4) tlačítko 📜 Pohyby bodů už neodhazuje do admin panelu — togglem načte posledních 10 pohybů inline (reuse `/admin/security/points-feed?q=<uid>`), odkaz na celý feed zůstal. Soubory: `app/routers/tickets.py`, `web/app.js`, `web/styles.css`, `tests/test_tickets.py` (+1 test: note 403 pro broadcastera, viditelnost jen pro staff, last_from_user přepíná po odpovědi, historie bez aktuálního ticketu). Ověřeno: pytest celá suite pass, `node --check`, browser QA na dev DB přes seedovanou admin session (filtr počítá správně 3 čekající, poznámka se uloží a renderuje, pohyby ±sedláky s důvody, konzole čistá; screenshot v preview opět timeoutoval — známý problém). Dev seed data uklizena. POZOR: změny jsou v pracovním stromu — příští `deploy.py --deploy` je odveze (jsou hotové a otestované, jen bez cache bumpu by starý app.js neviděl nové UI — deploy.py bumpne sám).

## 2026-07-18 (Claude) — Sedlák místo mince v topbar zůstatku
Uživatel chtěl v pilulce se zůstatkem („34 404 sedláků") místo oranžové mince maskota sedláka. Čistě CSS: `web/styles.css` — nové pravidlo `.pts-pill .coin` (24×24, `margin:-4px 0`, background `/sedlak-cut.png`), obecná `.coin` (ceny v shopu, kosmetika) zůstává mince. Ověřeno v preview přes DOM (computed style: pilulka = sedlak-cut.png 24px, shop cena = coin.png; screenshot opět timeout — známé). Seedovaná admin session smazána. Změna jen v pracovním stromu, nedeployováno.
**Update (stejný den):** uživatel chtěl sedláka VŠUDE místo mince → základní `.coin` pravidlo teď = `/sedlak-cut.png` 18×18 (border-radius 0), `/uploads/coin.png` + gradient pryč; `.pts-pill .coin` override zúžen jen na velikost (24×24, margin). Ověřeno DOM na shop stránce (7 ikonek, všechny sedlak-cut.png). POZOR lokální cache: starý CSS držel service worker + HTTP cache pod `?v=` klíčem — na produkci vyřeší deploy bump verze + CF purge.

## 2026-07-18 (Claude) — Zrušen blok darování „stejná IP/zařízení" (na přání provozovatele)
Uživatel chtěl zrušit hlášku „Účtu ze stejné sítě nebo zařízení poslat nelze" u Exchange darů. Odstraněn `_shared_identity` hard-blok v `app/routers/misc.py::gift_points` (dřív 403 při sdílené IP/otisku). Zbytek pipeline beze změny: 48h age-gate na nové účty ZŮSTÁVÁ, dary pořád jdou přes admin escrow schválení a admin panel dál ukazuje příznak `shared` (funkce `_shared_identity` žije, používá ji `/admin/gift-requests` + test_security_linked). Testy `tests/test_gift_antifarm.py` přepsány: `test_same_ip/device_cannot_gift` → `_can_gift` (nově čekají 200). pytest 4/4 pass. Revert: vrátit `_shared_identity` gate (viz ponytail komentář na místě). NENASAZENO — čeká na „nasaď".
POZN.: proběhla forenzní investigace altů uživatele KakaduCzech (id 335) — cluster 23 účtů (2 alty na vlastním PC + 20účtová Dolphin-Anty farma z 3.7., svázaná společným fp otiskem i přes per-profil proxy IP). Zrušení tohoto bloku funnel přes stejnou IP/zařízení nově umožní (admin ho ale u darů pořád vidí přes `shared`).
